---
topic: Talenta HRIS x Hyperledger Fabric Integration PRD
updated: 2026-08-11T22:50
---

- (decision) Real-names register (Talenta) approved by user; workspace bound as new run separate from ratified thesis PRD (2026-08-02) and Caliper dashboard PRD (2026-08-10). Coaching path, Vision+Features entry point, launch/production-grade stakes.
- (decision) API surface grounded from /Users/chan/Downloads/employee-profile-api-documentation.md (Talenta Core, 726 lines): endpoints across Personal/Employment/Education&Experience/Additional Info/Payroll domains under controllers MyInfoController, FormalEducationController, InformalEducationController, WorkingExperienceController, AdditionalInfoController, PayrollComponentController. Employee id = 'id' (User row), company id = 'company_id' (server-injected on payroll writes, never client-supplied). No versioning/rate-limit/generic correlation-ID scheme documented; auth is role/feature-flag based (Admin/Super Admin/Consultant/Employee), not token/API-key. Two documented auth gaps: InformalEducationController skips base login guard; /additional-info/index doesn't verify target employee's company membership.
- (decision) talenta-core HF001 branch (Yii2 PHP, NOT Laravel) already has READ-PATH-ONLY Fabric integration: BaseFabricBridgeService (Guzzle, X-Api-Key/X-Company-ID headers) -> BlockchainAnchoringService.getTransactionHistory() -> GET /v1/profile-sections/history on integration-bridge; MyInfoBlockchainHistoryService formats per-section (PERSONAL/EMPLOYMENT/EDUCATION/ADDITIONAL/PAYROLL); Vue SPA TransactionHistory.vue behind feature_my_info_blockchain_history flag; config keys FABRIC_BRIDGE_SERVICE_URL/API_KEY/COMPANY_ID. WRITE-PATH (anchoring profile changes into Fabric) is NOT implemented -- branch commit message confirms 'bridge + read-path only so far'. This PRD's primary net-new scope is the write path plus authorization enforcement.
- (decision) Existing hook point for write-path: every mutating MyInfoController action (actionUpdatePersonalData, actionUpdateEmploymentData, actionRequestChangePersonalData, actionUpdatePayroll, etc.) already calls EmploymentUpdateWebhookService::setUserId()->publish(), which pushes a WebhookWorker queue job -- this is the natural outbox/event point to extend for Fabric anchoring rather than introducing new infra. Auth in talenta-core is Yii2 session-based (Yii::$app->user, CustomController guards hasAccess()/checkUserId()), not token-based.
- (decision) Scope: all 5 data domains (Personal, Employment, Education&Experience, Additional Info, Payroll) anchored from day one -- no domain-priority phasing by user choice. Authorization model is BOTH preventive (Fabric consulted/blocks before Talenta DB write) and detective (tamper/divergence detectable after the fact). Blockchain Transaction History screen must be visible to every employee in the company regardless of role (needs disambiguation: own-record-only vs cross-employee visibility).
- (decision) Major scope simplification vs. original brief section 9: authorization enforcement is DETECTIVE ONLY, not preventive-blocking. Fabric is NOT consulted synchronously before a Talenta DB write is allowed -- Talenta's own existing role/feature-flag auth (Admin/Super Admin/Consultant/Employee, hasAccess()/checkUserId()) continues to gate writes as it does today. Fabric's job is to anchor the authorized change post-write (async, via existing EmploymentUpdateWebhookService->WebhookWorker hook) and make any out-of-band/unauthorized tampering (e.g. direct DB edit bypassing the app) detectable after the fact via hash-chain mismatch. Rationale (user, 2026-08-11): avoids Fabric becoming a blocking SPOF / added latency on every profile write. This changes brief sections 6, 9, 10, 11 substantially -- no synchronous ledger-state check in the write path.
- (decision) Blockchain Transaction History screen: every employee can view their OWN transaction history regardless of role (not admin-gated) -- but NOT cross-employee visibility. This matches Talenta's existing self-or-admin access model, extended so the feature itself isn't role-restricted for the self-view case; admin/auditor cross-employee viewing still follows existing company-scoped admin permissions.
- (event) Full draft PRD written (sections 1-16: Vision, Current/Proposed State, Goals/Non-Goals, Actors, Data Classification, FRs (FR-1..FR-13), NFRs (NFR-1..NFR-7), Authorization Model (D1 detective-only), Data Flows, Failure Handling, Migration Phases, Risks/Open Questions, Data Privacy, Acceptance Criteria AC-1..AC-6, Out of Scope). Open items flagged: NFR-1/2/3 numeric targets unconfirmed, NFR-7 reconciliation cadence unconfirmed, pre-existing Talenta auth gaps tracked as risk not fix scope.
- (decision) NFR-7 reconciliation cadence set to daily. Talenta's two pre-existing auth gaps (InformalEducationController login-guard skip; /additional-info/index missing company-membership check) confirmed as tracked risk, NOT fixed in this PRD's scope -- user approved leaving as risk item, not silently decided.
- (decision) NFR-2/NFR-3 written with measured dev-environment Caliper baseline (pt1-diag-20tps-5w: p95~26.7s, p99~27.7s single-write commit) explicitly disclaimed as non-representative of production hardware (per qa-tests/performance/RESULTS.md's own caveat: single 8vCPU/16GB laptop, peers at 91-128% CPU pre-load). Production numeric target marked TBD pending dedicated-hardware benchmark, per user instruction not to fabricate false-confidence numbers. NFR-7 set to daily reconciliation; auth-gap risk left as tracked risk not fix scope -- all confirmed by user 2026-08-11.
- (change) Input reconciliation (3 agents: API doc, talenta-core codebase, fabric-hris design) folded into PRD: (1) removed dangling addendum reference in FR-1; (2) FR-1/FR-1a corrected -- 4 mutations (actionDeletePayroll, actionAddComponent, actionDeleteEmergencyContact, actionImportDataEmergencyContact) never call publish() today, must add new call sites not just extend; (3) WebhookWorker is single-purpose/hardwired to postDataMessage with swallow-all catch, incompatible with FR-12/13 -- PRD now specifies a NEW job class on same Yii2 queue infra, not riding inside WebhookWorker::execute(); (4) added 3rd pre-existing auth gap (update-identity-address skips canRequestChangeData check) to SS9/13; (5) SS6 Data Classification expanded to cover Family (no_ktp) and Emergency Contact (phone_number) sub-tabs, previously only Basic-info was covered; (6) FR-4 added design note: operation type must derive from webhook payload semantics, not HTTP verb (verbs are frontend convention, mostly unenforced per API doc); (7) chaincode framing corrected -- ProfileSection enum already ratifies the same 5-domain split, no schema extension needed, reframed as positive finding not open risk; (8) FR-2/SS11 added note that BaseFabricBridgeService::send() swallows all errors into a plain array with no retryable/non-retryable distinction, new write client must add its own classification; (9) softened feature-flag citation as unconfirmed-exact-location, spot-check flagged.
- (change) Reviewer Gate (rubric walker: strong overall, 1 high/3 med/4 low; adversarial security: 1 critical/2 high/2 medium) resolved by strengthening design, not just disclaiming: (1) FR-11 amended to require reconciliation source 'what changed' independently of the anchoring trigger (DB updated_date, not webhook success log) -- closes critical F1 (attacker suppressing anchor also blinding detection); added AC-8 for this. (2) New explicit 'Detection boundary' subsection in SS9 distinguishes out-of-band tampering (caught) vs in-band-improperly-authorized writes (not caught, by design). (3) update-identity-address auth gap (F3, laundering) elevated from tracked-risk to launch pre-requisite for Personal domain specifically -- other 2 gaps remain risk-only. (4) NFR-7 split: hourly for Personal+Payroll (highest sensitivity), daily for the other 3 domains; removed NFR-7 from SS13 Open Questions (was contradicting its own SHALL in SS8). (5) FR-3 CREATE-time genesis-hash sentinel specified. (6) FR-8 boundary case made testable + AC-7 added. (7) Key-material isolation for new Blockchain Integration Service identity and FR-10 deleted-record behavior added as SS13 RISK/OPEN items for downstream security-architect. (8) Added SS16 Glossary (Domain/Anchor/Digest/Correlation ID/Reconciliation/Dead-letter). Full reviewer files: review-rubric.md, review-adversarial-security.md.
- (decision) FR-10 deleted-record behavior resolved: latest DELETE anchor -> report 'deletion verified' and stop, no hash-compare against absent DB state; re-appearance caught by FR-11 missing-anchor path instead. Marked [RESOLVED] in SS13.
- (change) Finalize step 4 (open-item triage) complete. 1 phase-blocker resolved (FR-10 deletion semantics). 5 non-blockers deferred with explicit revisit conditions, not silently dropped: NFR-1 <10ms latency assumption (revisit: stakeholder/perf-owner confirmation before build), NFR-6a backlog threshold (revisit: Phase 4 production hardening per SS12), NFR-2/NFR-3 numeric targets (revisit: dedicated-hardware Caliper benchmark), 2 lower-severity Talenta auth gaps -- InformalEducationController login-guard skip + /additional-info/index company-check gap (revisit: separate Talenta-side defect tickets, tracked risk not blocking), Blockchain Integration Service key-material isolation (revisit: security-architect agent's downstream threat model). All confirmed by user 2026-08-11.
- (change) Finalize step 5 (Polish) complete: structural pass cut redundant SS17 Out of Scope (pure restatement of SS4 Non-Goals), fixed stale 'schema extension' wording in SS2 to match the ProfileSection-enum finding, relocated positive chaincode finding from Risks section into SS6 where it belongs. Prose pass found no genuine comprehension issues (checked for repeated words/awkward joins; none found). Step 6 (external handoffs) skipped -- none configured.
- (event) PRD finalized. status: final, updated: 2026-08-11.
- (change) MAJOR CORRECTION incoming (2026-08-11, discovered during bmad-architecture session): integration-bridge/ (fabric-hris side) already implements the write path end-to-end -- POST /v1/profile-sections/{PERSONAL,EMPLOYMENT,EDUCATION,ADDITIONAL,PAYROLL} routes in cmd/integrationbridge/main.go, each calling a fixed hooks.* function (UpdatePersonalData, ApproveEmploymentTransfer, RecordEducationHistory, ApproveFamilyDataChange, UpdatePayrollBankAccount) through write-path-integration/writepaths -> gatewayclient.SubmitRecordProfileSection (real SubmitTransaction, not Evaluate). PRD's premise 'write-path anchoring is not implemented anywhere' was only verified against talenta-core (PHP) -- true that nothing calls the bridge yet, false that the bridge itself doesn't exist. Also found: bridge is PURELY SYNCHRONOUS, no queue/retry/idempotency/dead-letter (contradicts nothing in D1, but means FR-12/13 responsibility has nowhere to live except the new Talenta-side job); no CREATE/UPDATE/DELETE operation-type dispatch exists (each route hard-wired to one chaincode hook); salt/key stores are IN-MEMORY ONLY (wiring.go explicit comment: restart loses every salt/employeeKey_i); ADDITIONAL domain's hook is named ApproveFamilyDataChange, which sounds like Personal-domain Family data, not Additional Info custom fields -- naming mismatch to verify, not yet resolved.
- (change) PRD corrected in place (SS1 correction block, SS2 write-path-Fabric-side row, FR-1a, SS10 write data flow, 3 new SS13 items: in-memory key stores RISK, no-operation-type-dispatch OPEN, ADDITIONAL/ApproveFamilyDataChange naming-mismatch OPEN). Core scope narrowed correctly: build talenta-core trigger only, harden existing bridge (persistence, retry/dead-letter in the new Talenta job, not the bridge). No new adversarial reviewer pass run for this correction since it's citing verified real code (file:line evidence from investigation agent), not a new design debate -- lighter close than full Finalize. frontmatter updated to '2026-08-11 (corrected)'.
- (event) PRD correction closed. Resuming bmad-architecture session for the spine.
