---
topic: Caliper performance-benchmark GUI dashboard (UX)
updated: 2026-08-11T01:03
---

- (decision) Fast path confirmed, creative tools skipped for Discovery. Sources: this dashboard's own brief.md/addendum.md + prd.md/addendum.md only -- the core thesis project's ARCHITECTURE-SPINE.md/epics.md/prd-2026-08-02 are NOT pulled in. Reference repo (caliper-gui-dashboard) confirmed as structural/tab-layout inspiration only, not visual style -- colors/typography left open for the UX designer to propose.
- (event) Full draft written: DESIGN.md (shadcn/ui delta -- navy primary + 4 semantic status colors incl. reserved violet for recorded-fallback mode, metric+data-mono typography roles) and EXPERIENCE.md (5-tab IA matching PRD features, mode-indicator as the single most important component, UJ-1 elaborated to screen level with a stall/switch-to-recording edge case, projector-viewing-distance as the primary accessibility concern).
- (change) Input reconciliation against the PRD found 5 gaps, most material: EXPERIENCE.md had invented an unscoped in-dashboard 'RECORDED' replay mode/state that contradicts the PRD -- the recorded fallback is actually a separate video switched to entirely outside the app. Fixed: removed the RECORDED mode-indicator state and its violet color token from both files; mode indicator is now Idle/Live only; Key Flow's edge case now describes switching away from the dashboard, not an in-app mode change. Also fixed: IA's 'live topology' wording corrected to 'static topology + live status overlay' (matches FR-3's actual scoping); added a standing tenant02-exclusion guardrail to both files' Component Patterns/Do's-and-Don'ts (previously just a passing parenthetical); added FR-2's explicit-zero rule and a staleness-mute treatment for Dashboard tiles to the metric-tile pattern; added the PRD §10 generic-register constraint to Voice and Tone.
- (change) Reviewer gate (rubric walker) found 1 critical + 3 high + 3 medium + 2 low across both spines. Fixed all 9: added dark-mode pairs for the 3 status colors (critical); fixed 3 unresolvable {rounded.full} refs to bare literal 'full' (high); added a topology-diagram component entry to DESIGN.md, previously undefined despite being a flagship view (high); added a missing 'Slow' State Patterns row (high); added an 'on request' Key Flow beat touring the 3 simplified tabs per SM-2 (medium); fixed a broken 'DESIGN.md Foundation note' cross-reference (medium); added an Inspiration & Anti-patterns section carrying the PRD's rejected-alternatives material (medium); added a shadcn-primitives-used-as-is note (low); added the scenario-selector's first-load default state (low).
- (event) spines finalized
