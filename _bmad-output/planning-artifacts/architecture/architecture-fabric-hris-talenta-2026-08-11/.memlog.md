---
scope: Talenta HRIS write-path Fabric anchoring integration (per prd-fabric-hris-2026-08-11)
purpose: build-substrate + companion solution-design doc for stakeholders
altitude: feature
updated: 2026-08-12T00:10
---

- (decision) Boundary decision (user, 2026-08-11): talenta-core's new anchoring job class POSTs to the existing integration-bridge (same one BaseFabricBridgeService already talks to for reads), which owns digest construction and Fabric submission via gateway-client. Talenta-side stays a thin client; integration-bridge owns the Fabric-facing logic. Mirrors existing read-path pattern.
- (question) Discovered integration-bridge/ (Go module, cmd/integrationbridge + internal/pipeline) already has route files for all 5 domains (personal/payroll/education/employment/additional) plus a history route -- NOT audited by the PRD's reconciliation (which only checked talenta-core PHP side + CLAUDE.md/asset.go). Dispatched investigation agent to determine whether the write-path RECEIVING side already exists here, before designing anything new that might duplicate/contradict real code. Blocking further coaching until this returns.
- (event) Paused architecture coaching to correct PRD (prd-fabric-hris-2026-08-11) premise via bmad-prd Update intent -- integration-bridge investigation showed write-path already exists on fabric-hris side. Will resume architecture session after PRD correction lands.
- (event) Resuming architecture coaching. PRD correction closed. Boundary decision (user's option 2, entry 1) still holds and is now validated by the investigation: new Talenta-side async job -> existing integration-bridge write routes -> writepaths -> gatewayclient. Retry/dead-letter/idempotency must live in the new Talenta job since the bridge has none.
- (decision) AD candidate: domain-taxonomy collision resolved. Existing write-path-integration/writepaths/writepaths.go:285-293 ApproveFamilyDataChange hardcodes profileSection='ADDITIONAL' for family/dependents data (per its own doc comment) -- but the real Talenta API's 'Additional Info' (custom_field_id/field_name/field_type/value) is a distinct domain, and Family is a Personal-domain sub-tab per PRD SS6 (already correctly classified there). User decision (2026-08-11): repurpose the ADDITIONAL profileSection slot for real Additional Info; move Family-data anchoring under PERSONAL. Concretely: (1) writepaths.go needs a NEW Hooks method (e.g. UpdateAdditionalInfo) that saves/anchors with profileSection='ADDITIONAL' for real custom-field values, wired as the new Dispatch target for POST /v1/profile-sections/ADDITIONAL in cmd/integrationbridge/main.go:79-80 (replacing ApproveFamilyDataChange there); (2) ApproveFamilyDataChange's call site changes to profileSection='PERSONAL' (or the method is renamed/removed and Family writes fold into UpdatePersonalData's existing PERSONAL path) -- exact mechanics (separate hook vs merged into UpdatePersonalData) still open, since Family is per-family-member repeating data not a flat field set like Basic Info. This is now confirmed in-scope implementation work for fabric-engineer, not just a mapping note.
- (question) OPEN: should Family-data anchoring be its own Hooks method (profileSection=PERSONAL, distinct call) or merged into UpdatePersonalData's existing call? Family is repeating per-member data (reference_id per family member) vs Basic Info's flat single record -- digest construction (FR-2) needs to handle both under one PERSONAL profileSection value. Needs a decision before Structural Seed / Capability map can be finalized.
- (decision) AD candidate: composite key confirmed. Family data stays profileSection=PERSONAL (no ProfileSection enum extension -- preserves PRD's positive finding). Key structure becomes (employee, profileSection, recordIdentity) where recordIdentity='self' for single-record domains (Basic Info, Employment, Payroll, Education aggregate) and recordIdentity=familyMemberReferenceId for each Family sub-record under PERSONAL -- each chains independently. This resolves the hash-chain (FR-3) ambiguity raised earlier without reopening schema-extension.
- (event) Draft spine distilled: AD-1 (single retry authority), AD-2 (bridge is sole Fabric-facing write component), AD-3 (composite key employee+profileSection+recordIdentity), AD-4 (ADDITIONAL/Family route repurposing), AD-5 (operation-type as caller-supplied metadata, no routing branch), AD-6 (idempotency key travels with request). 2 [ASSUMPTION] tags (Additional Info recordIdentity shape; DELETE null-newValue validate.go allowance). Deferred: reconciliation placement, idempotency dedup mechanism, persistent key store (existing hardening task), dead-letter operator UI. Awaiting user review before Finalize (reconcile/reviewer gate).
- (decision) Both [ASSUMPTION] tags resolved by user (2026-08-11): (1) DELETE anchors carry newValue = last-known state before deletion, not null/empty -- validate.go's JSON-object check needs no change, DELETE payload shape identical to UPDATE's, distinguished only by operationType field. (2) Additional Info custom fields chain independently: recordIdentity=custom_field_id per field, matching the API's per-field save array shape. AD-3 and AD-5 updated in spine; removed resolved item from Deferred.
- (change) Finalize step 3 (Reviewer Gate) + step 2 (reconcile) complete. 5 agents: reconcile-prd (8 gaps), reconcile-code (5 gaps, 1 major blocker), review-rubric (conditional pass, 2 major/3 minor), review-web-verification (pass, versions confirmed real via go.mod), review-adversarial (5 holes, 2 critical). All folded into spine directly: AD-3 rewritten to encode recordIdentity into employeeID pseudonym instead of a nonexistent chaincode metadata slot (the one true blocker -- user chose this over a ratified schema change); AD-5 operationType moved off-chain-only (disclosed scope limitation, mirrors PRD's Detection Boundary pattern); AD-6 tightened to fix dedup OWNERSHIP not just mechanism, backstopped by chaincode's own NoOpOnIdenticalDataHash idempotency; new AD-7 (4 unwired MyInfoController actions must get NEW trigger calls, FR-1); new AD-8 (FR-10 on-demand verification resolves same pseudonym as AD-3, carries forward PRD's DELETE-verification [RESOLVED] rule); Consistency Conventions wire-shape fixed to include company_id/sourceEndpoint (FR-4) and recordIdentity wire format (bare string, no prefix) + operationType vocabulary (CREATE/UPDATE/DELETE uppercase); citation fixed validate.go:44->60-62; Structural Seed corrected to show chaincode composite key stays unchanged; 3 Deferred bullets expanded (NFR-7 hourly/daily cadence split, FR-13 dead-letter persistence model not just UI, Personal-domain launch-gate dependency on Talenta auth fix). lint_spine.py clean (0 findings) after all fixes.
- (event) Finalize step 5 (Renderings & polish) complete. Wrote SOLUTION-DESIGN.md (stakeholder-facing narrative companion, plain language, mirrors the 8 ADs without jargon). Structure/prose review: no changes needed on either pass.
- (event) spine finalized
