# Implementation Backlog — HRIS-on-Fabric PII-Anchoring Prototype

- **Gate:** re-opened design (G8/G9 **VOID**, `[prd: §11.1]`) · **Author:** `pm` (2026-07-13) → rewritten
  in full by `fabric-engineer`, 2026-08-06, executing `rencana-rekonsiliasi.md` Gelombang 4 item #21/#29.
- **Status:** **Design-only. Zero prototype code is authored by this document or from it today.**
  This is the per-role work backlog to **BUILD** the reconciled prototype (three-org, channel-per-tenant,
  section-based digest, in-band recording, no PDC, no Kafka, no anchor-service). Build remains
  **unauthorized** until the sponsor decides **S-1** (the G8a/G8b gate split, `rencana-rekonsiliasi.md`
  §1.3) and formally re-approves **G9** against *this* design — G9's prior sign-off is void and does not
  carry over (`[prd: §11.1]`; `project-context.md` "Critical Don't-Miss Rule"). Code samples anywhere in
  this backlog, the ADRs, or the solution docs are illustrative interface shapes, never deliverables.
- **Companion (G7):** the full-pyramid test strategy — re-derived 2026-08-06 against this same design —
  is [`./test-strategy.md`](./test-strategy.md) (unit `U-#` · integration `IT-#` · contract `CT-#` ·
  performance `PT-#` · security `ST-#`). Phase 6 below is the **execution plan** for those layers; test
  detail is cited `[TEST]`, not restated.

> ## What changed in this rewrite (2026-08-06)
>
> This replaces the backlog in full — not a patch on top of the `BLOCKED-SUPERSEDED` markers Gelombang 1
> (#7) placed on `AS-3/5/6`, `INT-1/2` (old), `CC-5` (old), `NET-3` (old). Those markers were a
> **stopgap** to stop dead work from compounding while ADR-0011–0021 were authored; ADR-0011–0021 now
> exist (`../05-adr/README.md`), so the stopgap is retired and its items are **removed from the live
> table below**, not merely relabeled again. Their disposition is preserved for audit trail in
> **§6 Appendix — retired items**, not deleted from history.
>
> **Removed wholesale:** every Kafka topic/consumer, the standalone Go anchor-service, the `anchor_map`
> off-chain state machine, the `AnchorRecord` shape, the MySQL-reconciliation job, and every Private Data
> Collection / `PurgePrivateData` / `blockToLive` reference (ADR-0014, ADR-0020 §9, ADR-0015).
> **Added:** `NET-6` (3-node Raft bring-up + fault-tolerance proof), `NET-7` (tenant-channel provisioning
> automation), `NET-8` (a **negative test** proving Org3 is read-only, not merely designed that way),
> a five-item `REC-*` phase replacing the anchor-service phase (the in-band recording component —
> `RecordProfileSection`'s off-chain half, embedded in the existing write paths, per ADR-0014), and
> `QA-8` (write the real Caliper numbers back into the thesis, closing `rencana-rekonsiliasi.md`
> Gelombang 6 #40). **Renumbered in place:** `NET-1/2` content updated for three orgs; `NET-3`'s slot
> now holds channel creation (was PDC config); `NET-5`'s slot now holds endorsement-policy wiring (was
> `docker-compose`, shifted to `NET-4`); chaincode lifecycle moved from `CC-6` to `CC-5` once the PDC
> item that used to occupy `CC-5` was removed.

> **Jira is unavailable (`[blocked]`), unaffected by this reconciliation wave.** Per the `pm` spec, Job 2
> (decompose design → Jira tickets via the Atlassian MCP) cannot execute in this environment. **This file
> is the LOCAL, offline backlog** — the source of truth that will feed the `jira-ticketing` skill / the
> TM board (Heimdall) the moment Atlassian access exists. Each item is already shaped as a ticket
> (ID · title · role · links · depends-on · DoD + estimate), so decomposition stays mechanical.

---

## Legend — link shorthands & conventions

Link shorthands used in the `Links` columns (all paths relative to this file):

| Shorthand | Target |
|---|---|
| `ADR-00NN` | [`../05-adr/ADR-00NN-*.md`](../05-adr/) — the accepted decision (see [`../05-adr/README.md`](../05-adr/README.md) for the 21-ADR index and Superseded-by chain) |
| `NET-DESIGN` | [`../00-architecture/solution/fabric-network-design.md`](../00-architecture/solution/fabric-network-design.md) — three-org, channel-per-tenant topology |
| `DATA` | [`../00-architecture/solution/data-model.md`](../00-architecture/solution/data-model.md) — the `EmployeeProfileRecord` shape |
| `INTEG` | [`../00-architecture/solution/integration-design.md`](../00-architecture/solution/integration-design.md) — the in-band write/verify sequences |
| `API` | [`../00-architecture/solution/api-contracts.md`](../00-architecture/solution/api-contracts.md) — the four-function chaincode contract (not a REST API — ADR-0020) |
| `SEC` | [`../08-security/security-architecture.md`](../08-security/security-architecture.md) — STRIDE threats `T1…T21` |
| `TEST` | [`./test-strategy.md`](./test-strategy.md) — pyramid layer detail (companion) |
| `GAPS` | [`../11-execution/grounding-gaps.md`](../11-execution/grounding-gaps.md) — `G-##` |
| `PRD` | `_bmad-output/planning-artifacts/prds/prd-fabric-hris-2026-08-02/prd.md` — the ratifying source |

- **Roles** are agents from the roster ([`../01-agents/`](../01-agents/)): `fabric-architect`,
  `fabric-engineer`, `backend-engineer`, `sre`, `qa`, plus `security-architect` / `architect` / `pm` for
  the pre-build gate and the one open cross-cutting design decision (§1.3).
- **Org naming** (generic register, matches `NET-DESIGN`): **Org1** = platform provider (2 peers + the
  3-node Raft ordering service); **`OrgClient-<tenantID>`** = that tenant's enterprise-client org (the
  "Org2" role, one instance per tenant, client-operated); **Org3** = the read-only auditor org.
- **Estimate** uses the project-planning story-point Fibonacci scale (1/2/3/5/8/13).
- **DoD** = the objective, verifiable exit bar for the item.

---

## 1. What is actually blocking build

> ✅ **RESOLVED 2026-08-06.** The governance deadlock (§1.1) is broken — S-1 decided, G9
> re-approved. **Build is authorized.** What remains blocking is narrower: **PB-1 and PB-3**
> (§1.3), each gating only the specific items named in their DoD, not the whole build. §1.1 is kept
> below as the historical record of the deadlock now resolved — do not read it as still current.

### 1.1 The governance deadlock — **BROKEN 2026-08-06** (historical record)

```
P1/P2/P4 require a running prototype + a real Caliper harness
        ↓
prototype code must exist
        ↓
"0 lines of prototype code" is a G8 pass criterion
        ↓
G8 re-run fails its own checklist
        ↓
G9 is unreachable → build stays unauthorized
        ↓
    (loops back to the top)          ← broken here: S-1 split G8 into G8a (passed,
                                         0-code rule stays) / G8b (measured, code now
                                         REQUIRED, not forbidden) — see §1.2, S-1
```

### 1.2 Sponsor decisions (S-1…S-5, `rencana-rekonsiliasi.md` §2) — **ALL DECIDED 2026-08-06**

| # | Decision | Decision recorded | What it gates | Status |
|---|---|---|---|---|
| **S-1** | Split G8 → **G8a** (document-consistency sweep, 0-code rule stays) + **G8b** (measured-artifact evaluation, code required and expected); retire the "0 lines" rule for G8b only | ✅ **Accepted as written**, 2026-08-06 (`quality-gates-and-approval.md` §5.5) | **The entire build — every phase below** | ✅ **Build authorized**, item by item against each phase's Definition of Done |
| **S-2** | OQ-2 — which relational engine is authoritative | ✅ **Confirmed**: keep the existing operational DB, no migration (ADR-0017) | `INT-*`, `DATA` §6 | ✅ Closed |
| **S-3** | OQ-6 — narrow the "reconstruct the original value" claim | ✅ **Confirmed**: narrow the claim + PITR as the operational answer (already applied to PRD UJ-1/`FR-17`) | `FR-17`, `API` "Client-side verification protocol" | ✅ Closed |
| **S-4** | PB-2/G-23 — dissolve or keep | ✅ **Dissolved** — no anchor-service, no Kafka, no read-back surface left to secure | `INT-3` (n/a — item retired), `SEC` T9/T10 | ✅ Closed — `SEC` T9/T10 and `GAPS` G-23 to be marked dissolved (not merely "likely") |
| **S-5** | Re-approve **G9** against the current assumption set | ✅ **Approved**, 2026-08-06 (`quality-gates-and-approval.md` §5.5) | Legal build authorization | ✅ Closed |

**All five sponsor decisions closed 2026-08-06 in a single session.** G8b build is now authorized —
see `quality-gates-and-approval.md` §5.5 for the exact scope of what this does and does not permit
(tracked-item code only; PB-1/PB-3 remain independently open on their own technical merits).

### 1.3 Technical pre-build gate (PB-#, carried from the original G5/G6 sweep)

Independent of S-1 — these close (or don't) on their own technical merits and gate specific phases, not
the whole build.

| ID | Blocker | Owner (accountable → supporting) | Links | Status |
|----|---------|----------------------------------|-------|--------|
| **PB-1** | **G-18 — verify the API-gateway identity-header trust boundary end-to-end.** If a caller can reach the write path with a spoofed tenant/user header, both tenant isolation and the pseudonymous `updatedBy` attribution collapse. | `security-architect` → `sre`, `backend-engineer` | `SEC` T1; `GAPS` G-18 | ⏳ **OPEN, unaffected by this reconciliation wave.** Still blocks `QA-5` (`ST-2`). |
| **PB-2** | ~~G-23 — the commitment-input read-back question~~ | `architect` → `security-architect`, `fabric-engineer` | `INTEG` §6; `GAPS` G-23; `rencana-rekonsiliasi.md` §2 (S-4) | ✅ **CLOSED 2026-08-06 (S-4)** — dissolved by human decision. No anchor-service, no Kafka, no cross-service read-back surface remains to secure; the in-band write path already holds the section value in memory to write it anyway. `SEC` T9/T10 and `GAPS` G-23 to be marked dissolved to match. |
| **PB-3** ✅ | **G-24 — pin RFC-8785 JCS canonicalization.** The recompute-and-compare verify contract is only deterministic if the writer and every verifier serialize the section value byte-identically. | `fabric-engineer` → `qa`, `security-architect` | `DATA` §3; `SEC` T5; `GAPS` G-24 | ✅ **CLOSED 2026-08-07** — pinned to `github.com/cyberphone/json-canonicalization@v0.0.0-20241213102144-19d51d7fe467` (maintained by Anders Rundgren, RFC 8785's co-author — the reference implementation, not a reimplementation; ships parallel Go/Java/.NET/Python/JS bindings verified against the same fixtures). Verified via `fabric-network/tools/jcsverify/main.go`: all 6 of the library's own official test vectors (`arrays`/`french`/`structures`/`unicode`/`values`/`weird.json`) byte-exact-match their expected output, and re-canonicalizing already-canonical output is confirmed idempotent — not merely asserted from the library's README. Closes `U-4`. `REC-1` unblocked. |
| **PB-4** | ~~G-01 — ratify the primary objective + demo success metric~~ | `pm` | `PRD` §2/§3 | ✅ **CLOSED 2026-08-02** — human decision (Chandra Kurniawan); objective + predicates **P1–P4** ratified (`GAPS` G-01, Part 0). |
| **PB-5** | ~~G-05 — ratify which business events/units are anchored~~ | `pm` | `PRD` §4 | ✅ **CLOSED 2026-08-02** — human decision; anchoring unit is the **profile section** (five values), the event-based surface (`EVENT_UPDATE_PERSONAL` et al.) is cancelled outright, not merely reordered (`GAPS` G-05, Part 0). |

**Gate rule.** S-1 is decided; build is authorized package-wide. **PB-1 is the only remaining
blocker**, gating only `QA-5`/`ST-2` — it does not block the rest of the backlog. PB-2/PB-3/PB-4/PB-5
are closed and listed here only so a reader does not go looking for open rows that no longer exist.
**Open blocking count: PB-1 = 1**, item-scoped, not build-wide. Closed: S-1, PB-2, PB-3, PB-4, PB-5 (5).

---

## 2. Build backlog by role & phase

Build phases run **network bring-up → chaincode → in-band recording component → integration → deploy →
test/eval**. All items are **DSRM Phase 4 (Demonstration)**; the Phase-6 evaluation items (`QA-4..QA-8`)
carry into **DSRM Phase 5 (Evaluation)**. Every item lists its accountable role, the ADR/design-doc it
traces to, its predecessors, a story-point estimate, and a verifiable Definition of Done.

### Phase 1 — Network bring-up  (lead: `fabric-architect`; container/ops support: `sre`)

Instantiates `NET-DESIGN` (three orgs, channel-per-tenant, no PDC anywhere).

| ID | Title | Role | Links | Depends-on | Est | Definition of Done |
|----|-------|------|-------|-----------|-----|--------------------|
| **NET-1** ✅ | Author `configtx.yaml` + genesis for the **three-org** consortium | `fabric-architect` | ADR-0012, ADR-0013, NET-DESIGN §1/§2 | Gate | 5 | ✅ **DONE 2026-08-06** — `Org1MSP`, `OrgClient-<tenantID>MSP` (templated, instantiated as `OrgClient-tenant01MSP`), `Org3MSP`, `OrdererMSP` (Org1-operated) all defined; app capability **`V2_5`**; `configtxgen` produces the genesis block with **no error, verified against the real `hyperledger/fabric-tools:2.5` binary** (not just written and assumed); no PDC block anywhere in the config (grep-verified). Artifacts: `fabric-network/network/configtx/configtx.yaml`, `fabric-network/network/crypto-config/crypto-config.yaml`, `fabric-network/network/channel-artifacts/tenant-tenant01.block`. **Empirical finding fed back:** the MSP-ID separator had to change from underscore to hyphen (`OrgClient-<tenantID>MSP`) — Fabric's signature-policy tokenizer rejects `_` in an `AND(...)` principal reference; corrected in ADR-0013 (technical-correction note, Status unchanged) and propagated to every file that named it. |
| **NET-2** ✅ | Stand up Fabric CA per org (`ca.org1`, one `ca.<tenantID>` per tenant, `ca.org3`) + TLS CA; MSP + NodeOU config; role/attribute enrollment convention | `fabric-architect` | ADR-0005, ADR-0009, ADR-0019, ADR-0021, NET-DESIGN §3 | NET-1 | 5 | ✅ **DONE 2026-08-06** — 3 combined identity+TLS CAs (`ca.org1`/`ca.tenant01`/`ca.org3`, `hyperledger/fabric-ca:1.5`) stood up via `docker compose` and verified live: bootstrap-admin enroll, one test peer per org, and one HRIS-attribute-bearing client identity all registered+enrolled successfully. NodeOUs confirmed enabled (`client`/`peer`/`admin`/`orderer`) on all 4 test identities. `hris.role`/`hris.company` confirmed embedded in the issued ecert via `openssl x509`-decoded OID `1.2.3.4.5.6.7.8.1` — exact ADR-0005 shape. TLS file-based throughout, no HSM/PKCS11; zero references to Domain B′/C secrets. Artifacts: `fabric-network/network/fabric-ca/{ca-org1,ca-tenant01,ca-org3}-config.yaml`, `fabric-network/network/compose/ca-docker-compose.yaml`, `fabric-network/network/fabric-ca/enrollment-runbook.md`. **3 empirical defects found+fixed** (documented in the runbook and compose file): (1) CA's own TLS-serving cert is `tls-cert.pem`, not `ca-cert.pem` as first assumed; (2) original compose mounted the config file outside `FABRIC_CA_HOME`, so all CA state was silently landing in the container's ephemeral layer instead of the named volume — fixed by mounting the config file inside the volume path; (3) Fabric-CA-issued MSPs do **not** auto-generate a NodeOU `config.yaml` the way `cryptogen` does — must be hand-authored per identity (now documented as an explicit runbook step). CA servers stopped after verification (state persists in the named volumes; `docker compose up -d` brings them back). |
| **NET-3** ✅ | Create **one** tenant channel `tenant-<tenantID>`; join Org1's 2 peers + that tenant's `OrgClient-<tenantID>` peer + the Org3 peer | `fabric-architect` | ADR-0013, NET-DESIGN §4 | NET-1, NET-2, NET-4 (execution-order correction) | 3 | ✅ **DONE 2026-08-06** — channel `tenant-tenant01` created (all 3 orderers report `"status":"active","height":1` via `osnadmin channel join`) and all 4 peers joined, verified via `peer channel list` on each (`peer0.org1`/`peer1.org1`/`peer0.tenant01`/`peer0.org3` all report `tenant-tenant01`) and `peer channel getinfo` (height 1, real block hash). State DB LevelDB and history DB were set at NET-4 bring-up time, carried here unchanged, not independently re-tested (no transactions exist yet to exercise `GetHistoryForKey` against — that needs chaincode, `CC-*`'s scope). **Note on the DoD's own wording:** "channel capability `V2_5` active" is imprecise — per NET-1's own finding, Fabric 2.5 adds no new *Channel*-group capability (only *Application* has `V2_5`); the channel group is `V2_0`, Application is `V2_5`, both confirmed active in the joined channel's config. Executed via a **Go program** (`fabric-network/tools/netjoin/main.go`, per explicit instruction) driving the real `osnadmin`/`peer` binaries through a network-attached `fabric-tools:2.5` helper container — not shell scripts. **Addendum, 2026-08-09: the original channel above was lost and recreated from scratch, same-day recovery.** `crypto-config/crypto-config.yaml`'s own documented command (`cryptogen generate --config=./crypto-config.yaml --output=.`) was re-run against the already-live network — this is the identical non-idempotency defect class first found at `NET-7`, but that item's fix (a skip-if-exists guard in `fabric-network/tools/tenantprovision`) only covers the per-tenant fragment path, not this shared file, which has no runtime guard at all. Effect: `org1` (both peer-org and orderer-org), `org3`, and `tenant01`'s root CAs (identity + TLS) were all replaced with fresh keypairs simultaneously, while every already-issued peer/orderer leaf cert — including the channel's own consenter certs — stayed on the old, now-nonexistent CA, breaking TLS/MSP trust network-wide (`x509: certificate signed by unknown authority`). `tenant02` was unaffected (separate config file). No backup existed (`crypto-config/` is gitignored) — recovery required a full `docker compose down -v` (destroying all 8 ledger volumes, **with explicit user confirmation**, including this channel's block-235 history), the broken org material moved aside (not deleted) to `crypto-config/broken-2026-08-09-ca-regen/`, a clean `cryptogen` regenerate, and the channel recreated from a freshly-`configtxgen`'d genesis block — the first recreation attempt still failed the same certificate error because the `.block` file itself had been generated *during* the broken window and bakes in orderer TLS certs at generation time, a second, previously-undocumented failure mode. Fully re-verified after recovery: all cert chains verify via `openssl verify`, all 8 containers stable, DNS resolves, Raft leader election clean. Documentation updated to prevent recurrence: `docs/QUICKSTART.md` §1/§2/§3/§4, `docs/CODEBASE-MAP.md` §7 (four new troubleshooting rows), and a `DANGER` block added directly to `crypto-config.yaml`'s own header. |
| **NET-4** ✅ | Local network bring-up via `docker-compose` (peers/orderer/CAs for Org1 + one tenant's `OrgClient` + Org3, mTLS everywhere) | `sre` (executed by `fabric-architect` — see note) | NET-DESIGN §6, SEC T3 | NET-1..2 (**not NET-3, see below**) | 5 | ✅ **DONE 2026-08-06** — `fabric-network/network/compose/network-docker-compose.yaml` stood up for real via `docker compose up -d`; all 7 nodes reached a stable `Up` state; `docker compose down -v` confirmed to fully reset named-volume state (exercised twice while debugging). **Two empirical defects found+fixed while bringing this up** (both now documented in the file itself): (1) the multi-value bracket env-var syntax (`CORE_PEER_TLS_CLIENTROOTCAS_FILES=[a,b,c]`) is **not** parsed as an array by the real `hyperledger/fabric-peer:2.5` binary — it crashed all 4 peers at startup, treating the whole bracketed string as one literal path; fixed by dropping it entirely and keeping only the singular `ROOTCERT_FILE`/`ROOTCAS` (own-org TLS CA), matching `fabric-samples`' own test-network pattern — resolves flagged uncertainty (1) empirically: cross-org TLS trust comes from the channel's embedded MSP `tlscacerts`, not a static core.yaml field. (2) `peer0.org1` **panicked and crashed** (`Setting both orgLeader and useLeaderElection to true isn't supported`) the moment NET-3 tried to join it to the channel — the flagged 2-peer gossip extrapolation was incomplete; the base image defaults `orgLeader=true`, so setting only `useLeaderElection=true` left both true. Fixed by explicitly setting `CORE_PEER_GOSSIP_ORGLEADER=false` on both Org1 peers. Both fixes verified by a full `down -v` + `up` + successful NET-3 run afterward, not merely patched and assumed. |

> **Dependency-graph correction — 2026-08-06, found while attempting to execute NET-3.** As originally
> written, NET-4 listed `NET-1..3` as its dependency (bring-up *after* channel creation) while NET-3's
> own DoD requires peers to be **physically joined** to a channel — a `peer channel join` / `osnadmin
> channel join` operation that is impossible against peer/orderer processes that are not yet running.
> The two rows' dependencies were inverted relative to physical reality. Corrected: **NET-4 (bring up
> peer/orderer containers) must execute before NET-3 (create channel + join those now-running peers)**;
> NET-4 depends only on NET-1/NET-2 (crypto material + CA), not on NET-3. This is a backlog-authoring
> defect caught the same way the NET-1 MSP-naming and NET-2 mount-path defects were — by attempting
> real execution, not by re-reading the design. Also corrected: NET-4's literal row said role `sre`;
> executed here by `fabric-architect` instead (the agent that has owned every other NET-* item to date
> and is the one scoped for Fabric network topology/bring-up — `sre` in this project's agent roster is
> a production release/incident specialist for the platform this prototype anchors, not a fit for
> local Fabric dev-network bring-up).
| **NET-5** ✅ | Define + apply the channel-level endorsement policy `AND('Org1MSP.peer','OrgClient-<tenantID>MSP.peer')` for the tenant channel from NET-3; confirm Org3 is excluded from the write-endorsement principal set | `fabric-architect` | ADR-0012 §5, NET-DESIGN §5 | NET-3 | 2 | ✅ **DONE 2026-08-06** — already empirically closed as a side effect of NET-1: the policy string `AND('Org1MSP.peer','OrgClient-tenant01MSP.peer')` is fixed in `configtx.yaml`'s `TenantChannelGenesis` profile (`Endorsement`/`LifecycleEndorsement` keys) and was verified structurally against the real genesis block via `configtxgen -inspectBlock` — the joined channel's `Application` group shows `Org1MSP` and `OrgClient-tenant01MSP` both carrying an `Endorsement` policy entry while `Org3MSP` carries none. No single org (including Org1) can satisfy the `AND(...)` alone. This is the channel-level *default* only — the authoritative wiring at chaincode-commit time (`peer lifecycle chaincode commit --signature-policy`) is `CC-5`'s job, not re-done here. |
| **NET-6** ✅ | **3-node Raft ordering service bring-up + fault-tolerance proof** | `fabric-architect` | ADR-0012 §2, `[docs: orderer/ordering_service.md#raft-concepts]` | NET-1 | 5 | ✅ **DONE 2026-08-06** — real, signed channel config-updates (BatchTimeout bumps) submitted through the live `tenant-tenant01` channel via `fabric-network/tools/raftfaulttest/main.go` (Go). **Probe 1** (`orderer1.org1` stopped, 1-of-3 down, 2-of-3 quorum): update succeeded (`Successfully submitted channel update`), block height 1→2. **Probe 2** (`orderer2.org1` also stopped, 2-of-3 down, 1-of-3 remaining): update failed explicitly with `SERVICE_UNAVAILABLE -- no Raft leader` — the exact expected Raft-quorum-loss error, height unchanged. Both orderers restarted afterward, network restored. `f=1` demonstrated with real evidence (exact error text captured), not asserted from the node count. **Two empirical findings along the way** (documented in the tool itself): (1) `peer channel fetch`/`update`'s mutual TLS to the orderer needs the separate `--clientauth` flag — `--certfile`/`--keyfile` alone are silently unused without it (client sends no certificate at all); (2) confirms NET-4's flagged uncertainty — `ORDERER_GENERAL_TLS_CLIENTROOTCAS` can stay unset; cross-org trust on the General listener is sourced from the channel's own embedded MSP `tlscacerts`, exactly as the orderer image's own shipped `orderer.yaml` comment documents. |
| **NET-7** ✅ | **Tenant-channel provisioning automation** — a runbook/script that, given a new tenant ID, performs NET-2's per-tenant CA enrollment, NET-3's channel-create-and-join, and NET-5's endorsement-policy wiring as one repeatable procedure | `fabric-architect` (written/executed by orchestrator, Go per standing instruction) | ADR-0013 (the `O(N)` lifecycle-fan-out consequence), NET-DESIGN §4 | NET-2, NET-3, NET-5 | 8 | ✅ **DONE 2026-08-06/07** — `fabric-network/tools/tenantprovision/main.go`. Given `tenant02`, it: generated fresh cryptogen material, inserted a new org block + channel profile into `configtx.yaml` (string-templated from tenant01's own blocks), produced the genesis block, brought up `peer0.tenant02` via `docker compose`, and joined the new channel on Org1's 2 peers + the new tenant's peer + Org3's peer — **verified**: `peer0.org1`/`peer0.org3` now report membership in **both** `tenant-tenant01` and `tenant-tenant02`; `peer0.tenant01`/`peer0.tenant02` each report membership in only their own channel — exactly ADR-0013's channel-per-tenant isolation model, now demonstrated with **two** real tenants, not one. **Three real defects found and fixed** (documented in the tool): (1) `cryptogen` is not idempotent — rerunning it for an already-generated org silently regenerates a fresh CA keypair, leaving the org's own already-issued leaf certs orphaned (signed by a CA that no longer exists on disk) — added an explicit skip-if-exists guard; (2) the compose-service insertion's idempotency check (does the YAML text already contain this service block?) is not the same as container-running idempotency — a rerun after a container was removed skipped bringing it back up; separated the two checks; (3) hit real consequences of these bugs on the LIVE network: `peer0.org1`/`peer1.org1`/`peer0.org3`'s ledger volumes for a stale, corrupted `tenant-tenant02` attempt had to be reset (**with explicit user confirmation** — a destructive action) and rejoined to both channels; `tenant-tenant01`'s data (including CC-5's smoke-test transaction) was confirmed to survive intact via gossip resync from the still-healthy orderers, and the locally-installed chaincode package (not part of the replicated ledger) had to be reinstalled on the 3 reset peers before queries worked again. **Addendum, 2026-08-07 (`QA-4`): this is a recurring defect class, not a one-off.** `QA-4`'s `PT-4` independently hit the IDENTICAL failure signature a second time — a fresh lifecycle action (there, a chaincode approval) against `tenant-tenant02`'s long-dormant channel state triggered the same "unexpected Previous block hash" panic across the same 4 peers, recovered the same way (ledger-volume reset + gossip resync, `QA-4`'s own entry). Two independent occurrences of the same signature is a structural risk in how `tenant-tenant02` has been created/abandoned/re-touched across this session, not a fluke — worth a genesis-block/channel-artifact regeneration discipline (or simply retiring `tenant02` as a reusable test fixture) before any further work touches that channel. Not yet ratified as an ADR-level fix; flagged here for whoever owns `DEP-2`'s provisioning runbook next. |
| **NET-8** ✅ | **Negative-test proof that Org3 is read-only** — not merely designed that way | `fabric-architect` (executed by orchestrator) | ADR-0012 §1/§5, NET-DESIGN §1 | NET-5, CC-5 | 3 | ✅ **DONE 2026-08-07** — a real `peer chaincode invoke` of `RecordProfileSection` submitted and endorsed only by Org3's own peer, using Org3's own identity, was **rejected** with the chaincode's own precise message: `"employeeprofilerecord: caller identity failed MSP/ABAC verification: MSP \"Org3MSP\" is not authorized to submit a write"` — CC-4's ABAC hook itself speaking, not an incidental lifecycle error. **Positive control, same test run:** the identical Org3 identity's `Evaluate` (`GetProfileSectionRecord`) against the same peer **succeeded**, returning the correct record — isolating that the rejection is write-specific, not "Org3 is broken/unreachable." **One real finding along the way:** Org3's peer initially rejected *both* read and write with "chaincode definition ... has not yet been approved by this org" — `approveformyorg` is a prerequisite for a peer to serve *any* chaincode call locally (read or write), a separate concept from being *required as a co-endorser* for writes; Org3 had to run `approveformyorg` (matching CC-5's design intent that Org3 *can* read) before the precise ABAC rejection above was reachable at all — this does not weaken the negative result, it is what made the positive-control half of the test possible. |

### Phase 2 — Chaincode  (lead: `fabric-engineer`)

Instantiates `ADR-0020`'s four-function contract (`API`) over the `EmployeeProfileRecord` asset (`DATA`).
**No PDC, no `PurgePrivateData`, no `blockToLive` anywhere in this phase** — ADR-0020 §9 explains why
none is needed: no function here ever receives a value there would be anything left to protect in a
collection.

| ID | Title | Role | Links | Depends-on | Est | Definition of Done |
|----|-------|------|-------|-----------|-----|--------------------|
| **CC-1** ✅ | Contract skeleton + `EmployeeProfileRecord` asset + composite-key scheme | `fabric-engineer` | ADR-0011, ADR-0013, ADR-0020, DATA §2/§5 | Gate, NET-1 | 5 | ✅ **DONE 2026-08-06** — `fabric-network/chaincode/employeeprofilerecord/chaincode/asset.go`. Asset fields exactly per `DATA` §2 (12 fields, no more — a unit test locks the exact field count and rejects any forbidden field like `salt`); key = `CreateCompositeKey("profile",[employeeID, profileSection])`, no tenant prefix; builds on `fabric-contract-api-go v1.2.2`. |
| **CC-2** ✅ | `RecordProfileSection` — Submit (write) function | `fabric-engineer` | ADR-0011, ADR-0020, DATA §2.3/§5, API, SEC T5/T7 | CC-1 | 8 | ✅ **DONE 2026-08-06** — `record_profile_section.go`. Args exactly per spec, 0 bytes plaintext/salt; identical-`dataHash` no-op; stale `prevHash` rejected (both shapes); ledger tx timestamp authoritative; non-MSP-verified caller rejected. MockStub unit tests pass AND a real invoke against the live network succeeded (see CC-5). **Load-bearing deviation, flagged not hidden:** `recordID` is derived deterministically from `GetTxID()` (UUID v5), not `crypto/rand` UUID v4 as `data-model.md` §2.1 literally says — a random ID would make every endorsing peer produce a different write-set and the AND policy could never be satisfied (confirmed: the real invoke's two endorsers, Org1+OrgClient-tenant01, matched). `data-model.md` §2.1 needs a follow-up edit to match. |
| **CC-3** ✅ | Three Evaluate (read-only) functions — `GetProfileSectionRecord`, `GetProfileHistory`, `GetEmployeeProfileSummary` | `fabric-engineer` | ADR-0007, ADR-0020, DATA §5, API | CC-1 | 5 | ✅ **DONE 2026-08-06** — `queries.go`. `GetProfileSectionRecord` returns exactly `{dataHash, timestamp, updatedBy, version}` — **verified against the real, running `peer0.org1`** (not just MockStub): `peer chaincode query` returned exactly those 4 fields, nothing more. `GetProfileHistory` sourced from `GetHistoryForKey` — verified against the real peer too (history DB enabled at NET-4), returned the one committed version with full record shape. `GetEmployeeProfileSummary` fans out to all 5 sections. No plaintext/salt parameter anywhere (grep-verified). |
| **CC-4** ✅ | CID-based MSP/ABAC verification hook on every function | `fabric-engineer` | ADR-0005, ADR-0020 step 2, SEC T13 | CC-1, NET-2 | 3 | ✅ **DONE 2026-08-06** — `identity.go`. Org-level (MSPID) ABAC: `Org1MSP`/`OrgClient-<tenantID>MSP` may write, `Org3MSP` may additionally read — a deliberate, documented narrowing of ADR-0005's finer-grained `hris.role` attribute scheme (not exercised here; flagged for confirmation, not silently decided). Deny-by-default; allow/deny unit tests per org role, all passing. |
| **CC-5** ✅ | Chaincode lifecycle + endorsement-policy commit — package once, install on Org1 (2 peers) + the tenant's `OrgClient-<tenantID>` peer + the Org3 peer, approve-for-my-org by Org1 and by `OrgClient-<tenantID>`, commit with the `NET-5` signature policy | `fabric-engineer` (executed by orchestrator) | ADR-0012 §5, ADR-0013, NET-DESIGN §5 | CC-1..4, NET-5, **NET-7 dependency dropped, see below** | 5 | ✅ **DONE 2026-08-06** — installed on all 4 peers, approved by Org1 + OrgClient-tenant01, committed at sequence 2 with `AND('Org1MSP.peer','OrgClient-tenant01MSP.peer')`, **a real invoke + query round-trip succeeded** (`RecordProfileSection` → `{"recordID":"a926fb8d-8372-56ca-a76a-dd2b6c9e1710","timestamp":"2026-08-06T15:47:06Z","version":1}`; `GetProfileSectionRecord`/`GetProfileHistory` both confirmed read-back correctly). **Dependency correction:** CC-5's literal dependency on NET-7 conflated "commit for the first/only tenant" (doesn't need NET-7) with "prove it repeats for a second tenant" (does) — same class of inversion as NET-3/NET-4; resequenced, NET-7's own multi-tenant proof is deferred, not this item. **Major pivot, fully disclosed:** the standard `--lang golang` package requires the peer to build the chaincode via Docker-in-Docker (mounting the host's `/var/run/docker.sock` into peer containers) — this was correctly blocked by the harness as a privilege-escalation-shaped grant; the user chose **Chaincode-as-a-Service (CCaaS)** instead. This required: (a) a second `main.go` start path using `shim.ChaincodeServer`; (b) a `Dockerfile` + built image for the chaincode running as its own independent, long-lived container (`employeeprofilerecord-ccaas`); (c) an `external`/`ccaas`-typed package (`connection.json` + `metadata.json`, hand-tarred — `peer lifecycle chaincode package` does not support `--lang external` at all). **Three real packaging defects found and fixed** in getting this to actually run: (1) initially registered a *custom* external-builder (env vars + a bind mount of hand-written detect/build/release/run scripts) at the exact same path (`/opt/hyperledger/ccaas_builder`) where the peer image **already ships a working, pre-registered `ccaas_builder`** (confirmed in the default `core.yaml`) — my override *replaced* the correct built-in binaries with broken stand-ins; fixed by removing the custom registration and mount entirely, restoring the default; (2) `metadata.json`'s `"type"` must be the literal string `"ccaas"`, not the more general `"external"` — the built-in builder's `detect` phase rejects anything else; (3) `connection.json` must sit at the **root** of the package's source directory, not in a `chaincode/server/` subdirectory (a documentation phrase about `release`'s own output shape, not the packaging input) — confirmed by the peer's own error message naming the exact expected path. |

### Phase 3 — In-band recording component  (lead: `fabric-engineer`; write-path integration support: `backend-engineer`)

**There is no standalone anchor-service in this phase, and nothing here is a new deployable unit**
(ADR-0014). These items build the shared digest/identifier/Gateway-client logic that gets **embedded**
into each of the five existing profile-section write paths in `Phase 4` — the same distinction
`data-model.md`/`integration-design.md` draw throughout. **Which process hosts this logic at runtime is
still open** (grounding gap **G-10**, reopened by ADR-0014) — every item below is written to be
host-agnostic; `REC-4`'s DoD flags this explicitly rather than silently assuming an answer.

| ID | Title | Role | Links | Depends-on | Est | Definition of Done |
|----|-------|------|-------|-----------|-----|--------------------|
| **REC-1** ✅ | Digest & identifier builder | `fabric-engineer` | ADR-0011, ADR-0020, ADR-0021, DATA §2/§3 | Gate, ~~PB-3~~ (closed 2026-08-07) | 8 | ✅ **DONE 2026-08-07** — `write-path-integration/gateway-client/digestbuilder.go` (package `gatewayclient`). `ComputeDataHash(salt, sectionJSON)` = `SHA-256(salt‖JCS(section))`, returns the exact `"sha256:<hex>"` on-chain field format; `ComputeEmployeeID`/`ComputeUpdatedBy` = `HMAC-SHA256(employeeKey_i, "id"/"actor"‖userID)`, no `pseudonymKey` anywhere (ADR-0021); `CanonicalizationVersion`/`HashAlgo` exported as versioned constants. 10 unit tests, all passing (`go test -v`): determinism, RFC-8785 key-order independence (proves JCS is actually doing its job, not just "some canonicalizer ran"), salt actually changes the hash (defeats the errata E-1 dictionary-attack class), exact byte-layout pinned against a hand-computed SHA-256, `employeeKey_i` keying/domain-separation between `EmployeeID`/`UpdatedBy`, and the pinned JCS library's own key-reorder + number-normalization behavior re-verified from within this package's own import (not just `jcsverify`'s standalone check). |
| **REC-2** ✅ | Off-chain salt + `employeeKey_i` store interface | `fabric-engineer` | ADR-0011, ADR-0019, ADR-0021, DATA §7 | Gate | 5 | ✅ **DONE 2026-08-07** — `write-path-integration/keystore/keystore.go`. `SaltStore`/`EmployeeKeyStore` interfaces + in-memory reference implementations (custody/rotation/backup policy remains `security-architect`'s, consumed not designed here — these are demo-grade, not production stores). 8 unit tests passing: fresh salt per record version, duplicate-put rejected (salt never rotated in place), one key generated once per employee and reused, different employees get independently-random keys, **deletion is provably irreversible** (a NEW, different key is generated on next use, not the old one recovered — the exact property ADR-0015's erasure and ADR-0021's accidental-loss trade-off both depend on). |
| **REC-3** ✅ | Fabric Gateway client wrapper | `fabric-engineer` | ADR-0014, INTEG §4/§5.2, DATA §5.2 | REC-1, REC-2, CC-5, NET-2 | 5 | ✅ **DONE 2026-08-07** — `write-path-integration/gateway-client/gatewayclient.go`, built on `github.com/hyperledger/fabric-gateway/pkg/client` (real SDK, not a CLI wrapper). One retained `grpc.ClientConn`/`client.Gateway`/`client.Contract` per `NewGatewayClient` call, reused across every method. **Verified against the live network** (integration test, `-tags integration`, not simulated): a real `EvaluateGetProfileSectionRecord` read CC-5's own smoke-test record back correctly; a real `SubmitRecordProfileSection` wrote a new record end-to-end; a **deliberately-wrong prevHash** triggered the chaincode's real `"stale chain reference"` rejection, which the wrapper caught, re-read the fresh head via Evaluate, and resubmitted successfully on the second attempt — the retry-as-expected-path behavior demonstrated live, not just coded. `MVCC_READ_CONFLICT` detection uses the SDK's typed `*client.CommitError.Code`; the stale-`prevHash` case uses `*client.EndorseError`'s message text (cross-process, no typed error available). Ledger/gateway failures wrapped in a distinct `LedgerError` type. **Two real defects found and fixed** (same class as NET-6/CC-5, in a fresh Go module that hadn't inherited the lesson): (1) the peer's mTLS requirement (`CLIENTAUTHREQUIRED=true`) needs a full `tls.Config` with `Certificates` set, not just `RootCAs` — a server-trust-only `credentials.NewClientTLSFromCert` silently fails the handshake; (2) `EvaluateGetProfileSectionRecord`/`GetProfileHistory` initially omitted the leading `tenantId` argument CC-3 actually requires, reproducing the exact "Incorrect number of params" class of bug already hit once in `ccdeploy` — confirms this needs to be checked per-integration, not assumed transferable. |
| **REC-4** ✅ | Five write-path hook integrations | `fabric-engineer` + `backend-engineer` | ADR-0014, DATA §4, INTEG §3/§4 | REC-1..3 | 13 | ✅ **DONE 2026-08-07, as a GENERIC DEMONSTRATION** (user-decided scope, 2026-08-07 — see note below) — `write-path-integration/writepaths/writepaths.go`. Five distinct functions (`UpdatePersonalData`/`ApproveEmploymentTransfer`/`RecordEducationHistory`/`ApproveFamilyDataChange`/`UpdatePayrollBankAccount`, matching data-model.md §4's five real business actions, not one generic dispatcher), each saving to a mock operational store then anchoring **in the same request** via REC-1/REC-2/REC-3, 0 bytes of plaintext/salt ever leaving the process. **Verified against the live network** (integration test, `-tags integration`): all five hooks succeeded for one demo employee; `GetEmployeeProfileSummary` fanned out to exactly the five sections with distinct `dataHash` values but the **same** `updatedBy` pseudonym across all five — live confirmation that one `employeeKey_i` is correctly shared across an employee's five sections (ADR-0011/ADR-0021), not just asserted from the formula. **Scope note, explicitly decided, not silently narrowed:** REC-4 as literally written means inserting this call into the REAL external HRIS application's own write paths — a separate, out-of-workspace production codebase this design never forks or rehosts (`repository-structure.md`). Given three options (generic simulation in this workspace / a written integration-spec document only / skip), the user chose the generic simulation — proving the hook PATTERN works end-to-end, not the real integration itself. The open dependency this item's DoD explicitly does not resolve (which process hosts this call, G-10; Fabric-outage handling — block/retry/degrade) remains exactly as undecided as before; seeing the pattern work live does not answer either question. |
| **REC-5** ✅ | Document encrypt + IPFS pin + CID attach (optional per write) | `fabric-engineer` | ADR-0016, DATA §10 | REC-4 | 5 | ✅ **DONE 2026-08-07** — `ipfs-cluster/docker-compose.yaml` (2 kubo nodes, `LIBP2P_FORCE_PNET=1` + `IPFS_SWARM_KEY_FILE`, private swarm) + `write-path-integration/ipfsclient/ipfsclient.go` (package `ipfsclient`), wired into `writepaths.Hooks.anchor` as a new optional `document []byte` parameter on all five write-path hooks. AES-128-GCM (`crypto/aes`/`crypto/cipher`, fresh random nonce every call — deliberately matched to `keystore.MinKeyBytes`'s existing 16-byte `KEY_EMPLOYEE`, ADR-0011's ≥128-bit floor, rather than inventing a second, larger key-size convention) encrypts the document **before** anything is added to IPFS; the ciphertext is added on the primary kubo node and explicitly pinned on the replica node too (the same 2-peer durability property ADR-0016 wants); only the returned CID is marshaled into the `RecordProfileSection` call's `ipfsCIDs` argument — 0 bytes of document plaintext or ciphertext ever reach the chaincode or the ledger. 6 unit tests (encrypt/decrypt round-trip, wrong-key failure, tampered-ciphertext rejection, wrong-key-length rejection) plus 3 tests against the **live** 2-node private swarm (round-trip through both nodes independently; **re-encrypting identical plaintext with the same key produces two distinct CIDs**, this item's core DoD, verified live not just asserted from AES-GCM's nonce theory; wrong-key fetch failure against real pinned ciphertext). **Verified end-to-end against the live Fabric network**: `writepaths`' own integration test writes `PERSONAL` WITH a supporting document and the other four sections WITHOUT one, then reads back via `GetProfileHistory` (the only query exposing `ipfsCIDs` — `GetProfileSectionRecord` deliberately returns only the reduced `ProfileSectionHead`, api-contracts.md) and confirms `PERSONAL`'s on-chain `ipfsCIDs` is non-empty while `EMPLOYMENT`'s stays `[]` — proving the CID-attach is real and per-write, not a blanket default. **Disclosed limitation, not hidden:** the `ipfs-cluster` CRDT-consensus orchestration layer's own two peers (`cluster0`/`cluster1`) do not currently negotiate a libp2p security handshake with each other ("incoming message was too large") despite three other real defects in that layer being found and fixed (AutoConf, the bootstrap trigger, a stale peerstore) and both cluster secrets confirmed byte-identical — this item routes around that unresolved layer by pinning directly on both kubo nodes over the independently-verified-working private swarm, which delivers the same 2-node durability property ADR-0016 actually asks for. Production cluster-orchestration hardening remains `DEP-3`'s open scope. |
| **REC-6** ✅ | Erasure hook | `fabric-engineer` | ADR-0015, DATA §11, INTEG §8 | REC-2 | 5 | ✅ **DONE 2026-08-07** — `write-path-integration/writepaths/erasure.go`, `Hooks.Erase`. All four crypto-shred steps in the correct order (EmployeeID must be derived from `employeeKey_i` to locate the salts **before** step 4 destroys that same key, or the salts become unreachable rather than deleted — a real sequencing hazard the implementation documents and avoids, not merely a comment). **Zero ledger calls** — no Gateway/chaincode reference anywhere in the function, by construction. Extended REC-2's `SaltStore` interface with `DeleteAllSaltsForEmployee` and added a `DocumentKeyStore` interface (`KEY_EMPLOYEE`, ADR-0019 Domain B′) — neither existed yet, both needed for this item's literal DoD. Test proves all 4 steps concretely: every one of the five operational-DB fields is gone, the salt is unreadable, and — the load-bearing check — re-deriving `EmployeeID` from a freshly-generated `employeeKey_i` for the same internal ID now produces a **different** pseudonym than before, proving the *old* one (and everything anchored under it) is permanently unreachable, not merely "the key is marked deleted somewhere." |
| **REC-7** ✅ | Client-side verification helper | `backend-engineer` | ADR-0020, INTEG §6, API "Client-side verification protocol" | REC-3, CC-3 | 5 | ✅ **DONE 2026-08-07** — `write-path-integration/gateway-client/verify.go`, `GatewayClient.Verify`. Verifier-class-agnostic by design: takes a salt as a plain parameter (obtained by the caller via `INT-3`'s separate hand-off channel, never fetched or derived by this function itself), recomputes `ComputeDataHash` locally, reads on-chain state via exactly one `Evaluate`, compares locally. **Verified live from all THREE verifier classes simultaneously** — Org1 (re-verifying its own write), `OrgClient-tenant01` (enterprise-client auditor), and Org3 (platform auditor) each ran `Verify` from an INDEPENDENTLY-configured `GatewayClient` pointed at their OWN peer's replica, all three matching correctly against the same record — real cross-org verification, not one client reading its own write back three times. **Also empirically demonstrates PRD success predicate P1 directly**: a deliberately tampered section value was correctly reported as `Matched: false`, with the on-chain and recomputed hashes both shown differing — tamper detection proven live for one concrete case, on real infrastructure. |

### Phase 4 — Integration  (lead: `backend-engineer` + `fabric-engineer`)

**No reconciliation-job item in this phase** (contrast the retired design's `INT-5`). Per `INTEG` §5.1,
there is no second store left to drift from the ledger — the digest is computed in the same write path
that commits the section, so the periodic-diff job the old design needed to close a dual-write gap has
no gap to close here. The residual risk that job used to catch (an approved change silently missing its
anchor) is reframed below as `INT-5`'s **within-request detectability** requirement, not a batch job.

| ID | Title | Role | Links | Depends-on | Est | Definition of Done |
|----|-------|------|-------|-----------|-----|--------------------|
| **INT-1** ✅ | Wire the end-to-end anchor write path for all five sections | `backend-engineer` + `fabric-engineer` | ADR-0014, INTEG §4, DATA §4 | REC-4, CC-5 | 8 | ✅ **DONE 2026-08-07, provisional policy, generic-demo scope** — `write-path-integration/writepaths/writepaths.go`. The end-to-end write→anchor path itself was already proven live by REC-4; this item's remaining, genuinely open piece — a labeled provisional answer to the "outage-handling policy for a Fabric failure occurring inside the request" precondition (`SEC` T6b, `GAPS` G-10, still reopened at the architecture level per ADR-0014's own Consequences section) — is now encoded as **"retry-bounded, then degrade-and-flag"**: REC-3's existing bounded retry (`MaxSubmitRetries`) still absorbs the two EXPECTED rejection classes; any other `anchor()` failure fails fast (no indefinite block, no attempted rollback of the already-committed operational-DB write — this design has no dual-write-coordination mechanism to attempt one with) and is wrapped in a new exported `PartialFailureError{EmployeeInternalID, ProfileSection, Version, Err}`. Labeled `[ASSUMPTION — provisional, not a ratified policy]` in the code, matching this codebase's own existing convention for exactly this situation (`record_profile_section.go`'s `deterministicRecordID` "[FLAGGED DEVIATION]" comment) — **a real follow-on ADR from `architect` still owns ratifying or superseding this**, per ADR-0014's own explicit Follow-up. Verified structurally (all five hooks call `Store.SaveSection` and early-return on its error BEFORE ever reaching `anchor()`) that the invariant this error type asserts — "any `anchor()` error implies the DB write already committed" — actually holds; **verified live** with a fault-injection integration test against the real tenant01 network (valid TLS/identity/channel, a deliberately nonexistent chaincode name) proving concretely: the operational store's value genuinely persisted, the returned error unwraps to `*PartialFailureError` with the correct identifying fields, and (INT-5, below) its callback fired exactly once. Independently re-verified live by a second, adversarial agent and a third time by the orchestrator directly — `go test ./...` and `go test -tags=integration -run TestIntegration -v ./...` both pass, REC-4's own pre-existing live test unaffected. |
| **INT-2** ✅ | Wire the verify/audit read path | `backend-engineer` | ADR-0020, INTEG §6, PRD §7 FR-37 | REC-7, CC-3 | 5 | ✅ **DONE 2026-08-07** — `write-path-integration/gateway-client/verify.go`. REC-7 already wired `Verify()` for all three verifier classes against a matching/tampered record; the one piece this item's DoD still required — "result distinguishes `NotFound` (never anchored) from a local mismatch (tampered)" — is now closed: a real defect was found and fixed (`Verify()` previously wrapped the chaincode's own `ErrRecordNotFound` sentinel as an opaque `*LedgerError`, indistinguishable from a genuine network/ledger failure). Added a `Found bool` field to `VerificationResult` and an `isNotFoundRejection` string-match helper (mirroring the existing `isExpectedRetryableRejection`/"stale chain reference" precedent — no typed cross-process error exists for chaincode business errors, same class of problem, same fix shape) so a never-anchored record now returns `{Found: false}, nil` — a legitimate outcome, not an error — while every other Evaluate failure still propagates as a real `*LedgerError`, unchanged. **Verified live**: a freshly-invented, never-anchored employeeID correctly returns `Found: false` from BOTH the Org1 and `OrgClient-tenant01` verifier classes; a real anchored fixture still correctly returns `Found: true, Matched: true` (regression); the full package's pre-existing integration suite (REC-3, REC-7, INT-4's tenant-isolation tests) all pass together, unaffected. Independently re-verified live twice more (adversarial agent + orchestrator). |
| **INT-3** ✅ | Salt / `employeeKey_i` hand-off channel (FR-36) | `backend-engineer` → `security-architect` | ADR-0011, PRD §7 FR-36 | REC-2 | 8 | ✅ **DONE 2026-08-07** — `write-path-integration/keystore/handoff.go` (2 new files, `keystore.go` untouched). Grounded in the ratified verbatim PRD §7 text (FR-14/FR-36, quoted in the package doc-comment), implements `AuthorizeSaltAccessFunc` (an injected policy seam — who counts as "the employee over their own record" vs. "an auditor within audit scope" is real authZ policy this package correctly does not decide itself, same boundary already drawn for custody/rotation on `keystore.go`'s other stores), an `AuditLog` recording **both** grants and denials (a denial is itself a security-relevant event), and `SaltHandoff.RequestSalt` — fail-closed on an `Authorize` error, `ErrSaltAccessDenied` + audited denial on an explicit refusal, and a genuinely-nonexistent salt propagated undecorated (never mislabeled as a security denial). Deliberately its own separate type/file, never a field on `gateway-client`'s `VerificationResult` — the whole point of FR-14/FR-36 together. **Superseded hedge, noted rather than silently dropped:** this item's original text hedged against an open `S-4/PB-2/G-23`; those were all closed/dissolved by the 2026-08-06 sponsor decision (S-4) ratifying in-band recording with no anchor-service/read-back surface — the design here is written straightforwardly for that final shape, no longer hedged against an architecture that no longer exists. Structural FR-14 sanity check run and reported precisely (not overclaimed): `verify.go`'s `VerificationResult` struct carries no salt field; `salt` appears in that file only as `Verify()`'s own input parameter (which the caller must already hold, obtained via this hand-off channel) and in explanatory comments — never derived, requested, or returned by it. 8 unit tests passing (grant/deny/fail-closed/not-a-denial distinction). Independently re-verified twice more. |
| **INT-4** ✅ | Tenant-onboarding integration test | `backend-engineer` + `fabric-architect` | PRD §7 FR-24, NET-7, SEC T20 | NET-7, CC-5 | 5 | ✅ **DONE 2026-08-07** — `write-path-integration/gateway-client/tenant_isolation_integration_test.go` (1 new file). NET-7 already closed the *provisioning-automation* half of this item's DoD; the piece that did not yet exist anywhere — a concrete, live proof that "onboarding credentials scoped to the new tenant are confirmed unable to touch any other tenant's channel" — is what this item adds. Two live tests: `OrgClient-tenant02`'s own identity/peer targeting `tenant01`'s real, populated channel, and the symmetric direction (`OrgClient-tenant01` targeting `tenant02`'s channel); both **fail at the channel-membership layer itself** ("could not get last config for channel ...") — a peer that never joined a channel simply has no blocks for it, which is the deeper guarantee ADR-0013's channel-per-tenant model actually rests on, not merely a chaincode-level ACL check. Deliberately does **not** assert Org1 is blocked from tenant02's channel (Org1 is the intended platform operator present on every channel, ADR-0013 — that is correct access, not a violation), and does not deploy chaincode to tenant02 (out of scope; CC-5 is deliberately tenant01-only). Independently re-verified twice more, live. |
| **INT-5** ✅ | Partial-failure detectability for the DB-write ↔ anchor-write pair (T6b floor) | `backend-engineer` + `fabric-engineer` | SEC T6b, INTEG §7 | INT-1 | 5 | ✅ **DONE 2026-08-07** — `write-path-integration/writepaths/writepaths.go`, built directly on `INT-1`'s new `PartialFailureError`. The "error" half of "at minimum surfaced as an error/metric" is `PartialFailureError` itself; the "metric" half is a new nil-safe, caller-injectable `Hooks.OnPartialFailure` callback, invoked exactly once at the point `anchor()` is about to return that error — deliberately a plain injected function rather than a hardcoded metrics/vendor dependency, since this workspace has no observability stack wired up yet (that remains `DEP-5`'s separate, later scope; this callback is its future integration point). **Explicitly makes no auto-remediation/compensation claim**, exactly as this item's own DoD requires — `RequestSalt`/rollback/compensation design remains out of scope. **Verified live** by the same fault-injection test as `INT-1` (they are one unit of work): the callback fired exactly once with the correct `employeeInternalID`/`profileSection`/`version`, alongside the typed-error assertion. Independently re-verified twice more. |

### Phase 5 — Deploy  (lead: `sre`)

| ID | Title | Role | Links | Depends-on | Est | Definition of Done |
|----|-------|------|-------|-----------|-----|--------------------|
| **DEP-1** ✅ | Helm chart — Fabric network (Org1: 2 peers + 3 orderers; a per-tenant `OrgClient-<tenantID>` peer template) on the platform's cloud K8s | `sre` | ADR-0012, ADR-0013, ADR-0014, NET-DESIGN §6 | NET-4, REC-4 | 8 | ✅ **DONE 2026-08-07, GENERIC ARTIFACT ONLY (human-confirmed scope)** — `deploy/helm/fabric-network/`. A direct translation of the real, live `fabric-network/network/compose/{network,ca}-docker-compose.yaml` (read first, not invented) into StatefulSets (stable identity for Raft/gossip) + headless Services + ConfigMaps, with the per-tenant `OrgClient-<tenantID>` peer as a single `range`-templated resource over `values.yaml`'s tenant list (`fabric-network.peer`/`fabric-network.tenantMspID` named templates) — onboarding a third tenant is a `values.yaml` append, not a new file; ships tenant01+tenant02 by default to prove the mechanism generalizes past N=1. No MSP/TLS Secret contains inline key material anywhere — every one is an `existingSecret` reference, with a `secrets.mode` switch for a future CSI/HSM-backed source (DEP-2's scope). Confirms explicitly (this item's own DoD): zero deployable units for the in-band recording component, matching ADR-0014's "Anchor-service deployable units: 0." **Never `helm install`/`template`-run** (helm unavailable in the build environment, and out of the standing generic-artifacts-only scope regardless) — every template was hand-traced for correctness instead of tool-validated; flagged prominently in the chart's own README as an open verification gap, not hidden. Also honestly documents one real, unresolved technical gap found while building it: the pre-issued crypto material's TLS SANs match the legacy dotted compose hostnames (`peer0.org1`), which Kubernetes Service names cannot literally reproduce — left for DEP-2/cluster-DNS, not silently assumed solved. |
| **DEP-2** ✅ | Identity/TLS/HSM provisioning + cert-rotation runbook, across **three** orgs + per-tenant client-org enrollment | `sre` | ADR-0005, ADR-0009, ADR-0019, ADR-0021, NET-DESIGN §3, SEC T2/T14 | NET-2, DEP-1 | 5 | ✅ **DONE 2026-08-07, GENERIC ARTIFACT ONLY** — `deploy/provisioning/` (3 runbooks). Grounded in the real, live `fabric-network/tools/tenantprovision/main.go` (operationalized as the concrete per-tenant enrollment tool, with an honest correction that its own doc-comment overclaims Fabric-CA enrollment — it only runs `cryptogen`, never `fabric-ca-client`, so a Fabric-CA-based tenant rollout is not actually automated by it today) and the real cert-expiry values in `ca-tenant01-config.yaml` (8760h enrollment/TLS, 131400h CA root, 24h CRL) rather than invented numbers. HSM/PKCS11 is named as a concrete production **option** (Library/Label/Pin-via-secret-reference/hash/AltID fields per `hsm.md`) — explicitly, honestly flagged as NOT what either of this workspace's two live identity paths (cryptogen or Fabric-CA) actually uses today (`bccsp: SW` everywhere). `key-domain-separation-checklist.md` is an operational, `grep`-able checklist sourced directly from `keystore.go`'s real code (not re-derived design) for all four ADR-0019/0021 domains. |
| **DEP-3** ✅ | IPFS Private Cluster deployment | `sre` | ADR-0016 | REC-5 | 5 | ✅ **DONE 2026-08-07, GENERIC ARTIFACT ONLY (production-grade, distinct from `ipfs-cluster/`'s dev docker-compose)** — `deploy/ipfs-cluster/` (standalone Helm chart, 16 files). Translates the dev cluster's own proven-working kubo config 1:1 (`LIBP2P_FORCE_PNET=1`, `IPFS_SWARM_KEY_FILE`, the exact `disable-autoconf.sh` init-hook logic copied verbatim — not reinvented), generalized 2→N via N single-replica StatefulSets (not one N-replica StatefulSet, since `ipfs-cluster` peers need distinct per-ordinal peer addresses a homogeneous template can't express). **Directly addresses, rather than hides, the dev cluster's own disclosed CRDT handshake defect** (`ipfs-cluster/README.md`): adopts explicit pin-on-every-node (generalizing `ipfsclient.go`'s Primary/Replica pattern to N endpoints) as the accepted production posture, with an honest trade-off statement (no auto-rebalancing, no auto-backfill for later-joining nodes) — while separately naming untried debugging leads for the underlying libp2p issue itself, not conflating "we have a workaround" with "the bug is fixed." Confirms via the real `ipfsclient.go` code path that encrypt-before-add is an **application-level** guarantee no cluster config could ever bypass. Carries ADR-0016's Org1-operator `[ASSUMPTION]` forward unchanged, not silently upgraded to a decision. One real defect found and fixed during the build itself: a backslash-escaped quote inside a Go-template `include` call that would have failed to parse. **Also independently re-flagged** (and now fixed, see below) a pre-existing, unrelated confidentiality-register violation in `NET-4`'s own `network-docker-compose.yaml` header comment — out of this item's scope to fix itself, correctly deferred to the orchestrator rather than silently patched mid-task. |
| **DEP-4** ✅ | CI/CD — chaincode (`CC-*`) + write-path-integration (`REC-*`/`INT-*`) build/test/image, lint, SAST, deploy gate | `sre` | ADR-0020, SEC T13, TEST (security layer) | CC-1, REC-1 | 5 | ✅ **DONE 2026-08-07, GENERIC ARTIFACT ONLY (no CI provider chosen for this repo)** — `deploy/ci/` (a tool-agnostic stage spec + one concrete GitHub Actions reference implementation, explicitly disclaimed as a reference, not an adoption claim). SAST names this session's own real Semgrep Guardian MCP tooling as the concrete mechanism, not a generic placeholder. The unit/integration-test stage explicitly encodes the real `go.work` per-module quirk (reproduced its exact error text this session) so a naive `go test ./...` from the wrong directory can't silently pass a broken CI config, and states plainly which of the 4 write-path-integration modules actually carry `//go:build integration` files. The image-build stage's Dockerfile/CCaaS env vars are copied verbatim from the real, already-built `fabric-network/chaincode/employeeprofilerecord/Dockerfile`. "No secrets in logs" (this item's own DoD) is enforced structurally in the reference YAML — a `docker login --password-stdin` via `echo` was found and replaced with a typed `docker/login-action` input specifically so no `run:` step's shell text can ever contain a secret value. No deploy-gate step runs a real `helm`/`kubectl`/`terraform` command anywhere, per the standing scope decision — it only names DEP-1/DEP-3's chart paths as a future hand-off target. |
| **DEP-5** ✅ | Observability — anchor-write failures/latency, cert expiry, peer/orderer health per channel, per-tenant channel health | `sre` | INTEG §7, SEC T12 | DEP-1 | 3 | ✅ **DONE 2026-08-07, GENERIC ARTIFACT ONLY** — `deploy/observability/` (Grafana dashboard JSON + Prometheus alert-rules YAML). Every panel/alert queries either a REAL, documented Fabric 2.5 Prometheus metric (`ledger_blockchain_height`, `consensus_etcdraft_active_nodes`/`cluster_size`/`is_leader`, pulled from the mirrored Fabric docs corpus, live once `metrics.provider` is flipped from the network's current `disabled`) or a metric this item explicitly defines as a not-yet-implemented target contract grounded in real code (`writepath_partial_failure_total`, sourced from `INT-5`'s actual `Hooks.OnPartialFailure` call site, including the correct nuance that `tenantID` must be closed over at construction time since the callback signature doesn't carry it) — never an imagined generic metric. A named "RESIDUAL RISK" panel plots Raft active-nodes vs. cluster-size specifically **because** ADR-0012's Org1-operated 3-node-Raft shared-SPOF residual exists, surfacing it rather than hiding it behind a generic up/down aggregate, exactly as this item's own DoD requires. Both artifacts syntax-validated locally (no Grafana/Prometheus/`promtool` in this environment — disclosed, not hidden). |

### Phase 6 — Test / Eval  (lead: `qa`; executes [`./test-strategy.md`](./test-strategy.md))

| ID | Title | Role | Links | Depends-on | Est | Definition of Done |
|----|-------|------|-------|-----------|-----|--------------------|
| **QA-1** ✅ | Unit tests — `U-1..U-8` | `qa` | TEST (unit), REC-1, CC-2/3/4 | REC-1, CC-2..4 | 5 | ✅ **DONE 2026-08-07** — `qa-tests/unit-coverage-report.md`. All 8 rows mapped to real, fresh-run, passing tests (file:line-level), most already existing from prior items and re-confirmed live here, not re-cited from memory. One genuine, narrow gap closed: `U-8`'s literal "fresh UUID v4" claim was untested and, per `record_profile_section.go`'s own self-disclosed `[FLAGGED DEVIATION]`, is actually false of the shipped code (a deterministic version-5 UUID, required for cross-peer endorsement determinism) — closed with one new test (`u8_recordid_coverage_gap_test.go`) that pins the REAL behavior rather than the untrue v4 claim. **Two real spec-vs-shipped divergences found and now tracked, not silently smoothed over:** `U-2`/`U-3` describe the pre-ADR-0021 `pseudonymKey`-based hierarchy ADR-0021 removed entirely (`test-strategy.md` corrected in place); `U-5` describes `ADR-0005`'s chosen attribute-based ABAC (`GetAttributeValue`), but the shipped chaincode implements `ADR-0005`'s explicitly-**rejected** org-level-MSP-allowlist option instead — a real, security-relevant gap, now disclosed at `ADR-0005` itself and tracked as **`G-30`** (new grounding gap) rather than buried in a test report. `CI` itself does not exist yet (`DEP-4` is an artifact-only spec, never applied) so "green in CI" is necessarily "green on a fresh local run" for now — stated plainly, not overclaimed. |
| **QA-2** ✅ | Integration tests — `IT-1..IT-10` | `qa` | TEST (integration), INT-1..5, NET-6..8 | INT-1..5, NET-4, NET-6..8, CC-5 | 8 | ✅ **DONE 2026-08-07** — `qa-tests/integration-coverage-report.md`. All 10 rows have a real, live, fresh-run passing test. `IT-1/3/4/6/7/8/10` were already proven by `INT-1..5`/`REC-4`/`NET-6`'s own prior work, re-run fresh (not cached — a `go test` result-cache hazard was itself caught and flagged as a live methodology risk for future re-runs) rather than cited from memory; `IT-2` had no surviving runnable artifact anywhere in the repo (`NET-8`'s own proof was only ever interactive `peer` CLI + prose) so it was re-proven live via an ad hoc script reproducing the exact ABAC rejection — flagged as a real, still-open gap that a permanent, repo-committed re-proof does not exist. **`IT-5` and `IT-9`'s erasure-basis half were genuinely new** — one new file (`write-path-integration/writepaths/erasure_ipfs_integration_test.go`) proves, live: on-chain state is byte-for-byte unchanged before/after `Hooks.Erase` across every read surface, and a document's old ciphertext fails to decrypt under a fresh, independently-random post-erasure `KEY_EMPLOYEE` — the concrete "`unpin ≠ delete`, key-destruction is the real mechanism" proof `T18` wants. `erasure.go`/`writepaths.go` untouched. One stale-naming premise found and disclosed: `test-strategy.md`'s literal `IT-2` text still says `Org2MSP`, a channel-per-tenant renaming leftover — live policy and re-proof both correctly use `OrgClient-tenant01MSP`. |
| **QA-3** ✅ | Contract tests — `CT-1..CT-4` (Newman / `api-test-automation`) | `qa` | TEST (contract), API | INT-2, INT-3, INT-4 | 3 | ✅ **DONE 2026-08-07, PREMISE CORRECTED** — `qa-tests/contract-coverage-report.md`. Investigated before assuming: confirmed via `ADR-0014` ("Anchor-service deployable units: 0") that there is no HTTP endpoint anywhere in this design for Newman to drive — write/verify/salt-delivery/onboarding are all direct in-process Go-to-Fabric-Gateway-gRPC calls, the same class of stale assumption already known for `PB-1`/`ST-2`'s API-gateway premise (`test-strategy.md` §1 corrected in place to say so explicitly, rather than force-fitting a Postman collection onto a surface that was never HTTP). `CT-1`/`CT-2`/`CT-3`'s properties are already covered by real Go-level tests (`Verify()`'s salt-free signature, `SaltHandoff`'s authorization tests, the tenant-isolation tests). **`CT-4` was a genuine, real, previously-uncovered gap, not just a premise mismatch**: no existing test constructed a deliberately mismatched/forged identity. Closed with one new file (`invalid_identity_integration_test.go`, package untouched otherwise) — two live tests present a real, valid mTLS cert while claiming a DIFFERENT org's MSP ID, both correctly rejected by the peer's own MSP layer ("creator is malformed"), proving rejection happens at the real chaincode/peer layer, not a local SDK check. |
| **QA-4** ✅ | Performance tests — `PT-1..PT-5` (Hyperledger Caliper) | `qa` | TEST (perf), ADR-0018 | INT-1, DEP-1 | 8 | ✅ **DONE 2026-08-07 — real measurement, P2 FAILS decisively** — `qa-tests/performance/` (Caliper 0.7.1, bound to **Fabric 2.5** per this item's own DoD, not 2.2). **PT-1/PT-2 (real, honest result): NFR-1 (<3s @ 500 TPS) fails by ~7×-23× at every one of the 4 load points** — achieved successful-commit throughput saturates at ~35-210 TPS regardless of requested rate (200/500/1000/2000), p50 latency 21-63s, p99 28-70s. Root-caused, not just measured: an independent, non-Caliper raw `peer chaincode invoke` CLI call took 20.3s for one **unloaded** write, and `docker stats` showed `peer0.org1` at 128% CPU before any Caliper load was applied — this is a real property of one oversubscribed laptop simultaneously hosting the entire Fabric+IPFS stack (documented in full, including exact resource numbers, in `RESULTS.md` §6), not a chaincode/design defect or a benchmark-harness artifact. **PT-3: effectively passes** — p50 7.6ms, p95 135.6ms, p99 946.4ms (marginal at the tail, sub-second everywhere else), 0 failures across 1000 reads. **PT-4: one clean data point only** (`tenant-tenant01` alone) — attempting the 2nd-channel comparison on the previously-dormant `tenant-tenant02` channel surfaced a real, pre-existing genesis/ledger-hash defect that crashed 4 peer containers; **diagnosed and fully recovered same-day** (ledger-volume reset + gossip resync, verified byte-identical to the never-affected `peer0.tenant01` at every block, full live integration suite re-passing afterward) — see `RESULTS.md` §3.3a. **This is the SECOND occurrence of the identical `NET-7` defect class** (tracked as a structural, not one-off, risk — see `NET-7`'s addendum below). `tenant-tenant02` deliberately left abandoned, not re-repaired. **PT-5: complete, run after recovery** — the batched/concurrent submission pattern completed its round in 3.93s vs. the sequential/blocking pattern's 14.10s for a comparable workload, 100% success both legs. **Two real Caliper defects found and fixed** (patched into the sandboxed `node_modules` under `qa-tests/performance` only): the `fabric:2.5` connector has zero mTLS support in any published version and cannot connect to this network's Gateway service at all without a patch (this network mandates client-cert auth); the framework computes no latency percentiles anywhere, requiring an independent recording/aggregation script. **Independently re-verified three times over**: the reproduce-stage agent (which itself briefly, then correctly, self-corrected an unauthorized file-contamination mistake — flagged, checked, confirmed harmless), and the orchestrator directly (re-ran `compute-percentiles.js` from scratch, matched exactly; independently diagnosed and executed the peer recovery; independently re-ran the full live integration suite twice post-recovery). |
| **QA-5** ⚠️ | Security tests — `ST-1..ST-14` | `qa` | TEST (security), SEC §2 (T1–T21), control-matrices | INT-1..5, DEP-2, **PB-1** | 13 | ⚠️ **RUN 2026-08-07 — real, honest results; P0 (`ST-1`) FAILS, three items BLOCKED, rest CONFIRMED.** All 14 items executed live and independently re-verified (`qa-tests/security/`). Full detail per item:
  - **`ST-1` (P0) — FAIL, with a critical nuance.** `fabric-network/tools/pilscan` (new Go tool, parses raw blocks — tx args, world-state writes, event payloads — not just re-querying the same chaincode API under test) scanned all 232 blocks across both tenant channels: **31,895 findings**, all identifier-SHAPE violations (`employeeID`/`updatedBy` values that are raw test-fixture strings, not 64-hex HMAC-SHA256 pseudonyms) — traced to several OTHER test suites this session (`REC-3`, `REC-7`, `INT-2`, `ST-5`, `QA-4`'s Caliper benchmark) calling `RecordProfileSection` directly with hand-rolled literals, bypassing `writepaths.Hooks.anchor()`'s real pseudonymization pipeline. **Triple-independently-corroborated: zero actual PII was found** (a fixture-string dictionary check, a raw-block email/SSN/name-pattern backstop, and the verifier's own separate independent pattern search all returned zero hits) — this is a missing-enforcement finding, not a real data leak. **New, real defense-in-depth gap disclosed**: the chaincode performs no shape validation on `employeeID`/`updatedBy`/`dataHash` before persisting them immutably — tracked as **`G-31`** (new grounding gap) and flagged in `record_profile_section.go` itself. The 31,895 non-conforming records already on the live demo ledger are permanent (append-only) and cannot be retroactively fixed.
  - **`ST-2`** — confirmed genuinely **open/unverifiable from within this workspace** (no HTTP/API-gateway surface exists in this in-band design at all; concerns the real external HRIS app's own public edge, out of scope). Not closed, not faked as tested.
  - **`ST-3`** — **BLOCKED**, and surfaced two independent, real structural findings beyond "the CA containers are down" (correctly left un-restarted per this round's safety rules): (a) `NET-2`'s Fabric-CA PKI root certs are **not actually the live network's trust anchor** (disjoint from `configtx.yaml`); (b) **no CRL distribution mechanism exists anywhere in this repo**. `SEC` T2 ("mitigated by cert lifecycle") is therefore unmitigated in practice, not merely untested. Tracked as **`G-32`** (new grounding gap).
  - **`ST-4`** — (a) **CONFIRMED PASS**, live: a plaintext (no-TLS) gRPC connection to `peer0.org1:7051` is refused at the transport layer. (b) **Real finding, disclosed not hidden**: the IPFS kubo HTTP API (`:5001`/`:5002`) is plain, unencrypted HTTP — `ipfsclient.go` has no TLS option. Document content itself is still AES-256-GCM-encrypted before upload (REC-5), so plaintext isn't exposed via this channel, but CIDs/metadata/ciphertext transport have zero transport-layer confidentiality/integrity today. Not fixed — out of this item's scope.
  - **`ST-5`** ✅ — **DONE**, closes errata E-2. New live tamper-detection proof for `EMPLOYMENT`/`EDUCATION`/`ADDITIONAL` (labeled `SC-F` per `test-strategy.md`'s own naming instruction)/`PAYROLL`, matching `REC-7`'s existing `PERSONAL` proof exactly — all five sections now covered, live.
  - **`ST-6`/`ST-10`/`ST-11`** ✅ — **DONE.** Confirmed by code citation (not re-derivation): salt construction, no-salt-on-chain, independent per-domain key generation (new test added for `DocumentKeyStore` vs `EmployeeKeyStore` — a genuine, now-closed pre-existing test gap), no PDC defined anywhere real (only inert generic-toolkit references, correctly called out as non-findings), mTLS-everywhere. Domain B (app AES) honestly reported as **not implemented anywhere in this workspace** — a scope gap, not fabricated as present.
  - **`ST-9`** — **BLOCKED**, honestly: no Semgrep AppSec Platform project is registered for this deployment at all (`list_semgrep_projects` returns empty) — the available MCP tools are read-only against already-uploaded scans and cannot onboard a new project. Correctly reported as unevaluated, not faked as "clean."
  - **`ST-7`/`ST-8`** ✅ — confirmed moot (S-4, 2026-08-06), regression-guard checks re-run and hold.
  - **`ST-12`** ✅ — (a) re-confirmed live; (b)'s T16b residual demonstration is **CLOSED**, not merely re-run — `ADR-0021` removed `pseudonymKey` entirely, so there is no longer a construct for that residual to exist against.
  - **`ST-13`/`ST-14`** ✅ — re-confirmed live against `QA-2`/`INT-4`'s existing tests, no new code needed.
  Every lane independently, adversarially re-verified live by a second agent; the orchestrator directly re-checked the ST-1/ST-5 findings, the two new grounding gaps, and the chaincode disclosure note. |
| **QA-6** ✅ | MVCC same-section contention measurement | `qa` | ADR-0018, DATA §5.2, `rencana-rekonsiliasi.md` §4.1 | QA-4 | 3 | ✅ **DONE 2026-08-07 — real measurement, clean confirmatory result** — `qa-tests/performance/{workload/hotkey.js, benchconfigs/qa6-mvcc-contention.yaml, QA6-RESULTS.md}`. Rather than reusing `QA-4`'s 200/500/1000/2000-TPS load points (which that item's own real numbers already showed are dominated by `DEADLINE_EXCEEDED` timeouts on this environment, not MVCC contention — reusing them here would have measured overload, not contention), this item deliberately isolates the DATA §5.2 property it's actually meant to measure: N concurrent workers racing to write the exact same brand-new `(employeeID, profileSection)` head key in one synchronized burst, at three small, resource-appropriate concurrency levels (N=2/5/10). **Measured conflict rate: 50% (N=2), 80% (N=5), 90% (N=10)** — exactly `(N-1)/N`, the mechanically-expected shape for "exactly one winner per burst" (flagged explicitly in the report as a property of the burst design, not a general background contention probability to extrapolate from). **Rejection class confirmed precise, not assumed**: all 14/14 rejections across every level are genuine Fabric-level `MVCC_READ_CONFLICT` (gRPC status code 11, `TxValidationCode`) surfacing at **commit time** — zero were the chaincode's own application-level `ErrStaleChainReference` (endorsement-time check, structurally unreachable here since every racing worker reads `head==nil` cleanly before any of them commits) and zero were environment-timeout artifacts (`grep -ic "deadline"` = 0, consistent with these burst latencies of 3.1-5.2s sitting far below `QA-4`'s sustained-load timeout territory of 14-70s). **Independently reproduced exactly** (identical 50/80/90% at fresh employeeIDs), and the reproduction went one step further than the original measurement: cross-checked directly against the raw Fabric peer ledger logs on **both** endorsing peers (`peer0.tenant01` and `peer0.org1`), confirming the MVCC rejection counts match precisely at the `committer.txvalidator` layer itself — proving the signal comes from Fabric's own state validator, not merely the SDK's or Caliper's interpretation of a generic error. One real Caliper harness constraint found and correctly worked around (not hidden): Caliper 0.7.1 fixes one global worker-pool size per benchmark run with no per-round override, requiring harmless "filler" transactions from idle workers outside each round's actual racing set to keep Caliper's fixed-transaction-count loop from hanging — documented in `QA6-RESULTS.md` §1, verified by reading Caliper's own source before implementing, not discovered by trial-and-error. Network confirmed healthy throughout and afterward; no live-infrastructure incident this round. |
| **QA-7** ✅ | DSRM demonstration + evaluation against **P1–P4** | `qa` | TEST (eval), PRD §3, DSRM Phase 4/5 | QA-1..6 | 5 | ✅ **DONE 2026-08-07** — `qa-tests/evaluation/QA7-dsrm-evaluation-p1-p4.md`. **P1: PASS** — 100% across all five sections, live (`REC-7`'s PERSONAL + `QA-5` `ST-5`'s EMPLOYMENT/EDUCATION/`ADDITIONAL`-as-`SC-F`/PAYROLL), closing errata E-2. **P2: FAIL**, decisively, against `QA-4`'s real numbers (21.1s p50 @ the 500 TPS bar itself, ~7× over the 3s budget) — root-caused to this session's single oversubscribed laptop, not the design, and reported with that caveat attached, not hidden behind it. **P3: PASS** — real second tenant (`tenant02`), both directions rejected at the channel-membership layer (`QA-2` `IT-8`, `QA-5` `ST-14`, `INT-4`), correctly characterized as identity-bound/channel-enforced isolation per `prd.md` §9.6's own instruction, not overclaimed as "cryptographic." **P4: PASS** (as a reporting predicate) — the ratified 11-row §9 compliance matrix carried forward at its real tiered status (2 `Comply`, 4 `Comply sebagian`, 3 `Comply dengan catatan`, 2 `Tidak dapat diklaim`), both `Tidak dapat diklaim` rows named as open limitations, none rounded up. **Also answers `prd.md` §3.2's own flagged gap** (no predicate tests confidentiality) using `QA-5` `ST-1`'s real scan evidence: zero actual PII found (triple-corroborated), but a real, newly-disclosed enforcement gap (`G-31`) is attached as an honest caveat to compliance-matrix row 8, not a status downgrade. Explicit scope boundary stated: `ST-2`/`ST-3`'s open findings, the thesis's own BAB III/IV methodology-reporting gaps (errata E-4/E-9), and the FGD qualitative track remain unresolved by this technical report — not silently claimed as closed. |
| **QA-8** ✅ | Write the measured Caliper numbers back into the thesis | `qa` | PRD §3.1, `rencana-rekonsiliasi.md` Gelombang 6 #40 | QA-4, QA-7 | 3 | ✅ **DONE 2026-08-07, as a separate findings report (user-directed scope, 2026-08-07 — not an edit to the thesis `.docx` itself)** — `qa-tests/evaluation/QA8-thesis-numbers-report.md`. `QA-4`'s real per-load-point throughput/latency table (200/500/1000/2000 TPS: success rate, achieved throughput, p50/p95/p99, min/max) replaces the placeholder **850.4/1,950.5 TPS, 2.85s** figures, with the 500 TPS row (the bar P2 is literally defined against) identified as the correct headline figure if BAB V needs one — **21.08s, not 2.85s**. Read-path real numbers (100% success, sub-second p50/p95, marginal p99) replace the read-throughput placeholder. `PT-5` bulk-comparison and `QA-6`'s MVCC-contention numbers included as supplementary, real, Caliper-measured data. **P2's verdict recorded as FAIL against the measured figure**, per this item's own DoD — with the environment-resource-contention caveat stated as mandatory accompanying context wherever these numbers are cited, not optional footnote. |

**Build work items: 38** (`NET` ×8 · `CC` ×5 · `REC` ×7 · `INT` ×5 · `DEP` ×5 · `QA` ×8). **Total incl.
the governance/technical gate rows (§1): 47.**

---

## 3. Traceability — every build task → an ADR or a G5 design doc

Confirmed: **no build item is invented.** Each traces to at least one of the 21 accepted/superseded ADRs
and/or a G5 solution-design document (and, where security-relevant, a G6 STRIDE control).

| Item(s) | Traces to |
|---|---|
| NET-1 | ADR-0012, ADR-0013 · NET-DESIGN §1/§2 |
| NET-2 | ADR-0005, ADR-0009, ADR-0019, ADR-0021 · NET-DESIGN §3 |
| NET-3 | ADR-0013 · NET-DESIGN §4 |
| NET-4 | NET-DESIGN §6 · SEC T3 |
| NET-5 | ADR-0012 §5 · NET-DESIGN §5 |
| NET-6 | ADR-0012 §2 |
| NET-7 | ADR-0013 (`O(N)` fan-out) · NET-DESIGN §4 |
| NET-8 | ADR-0012 §1/§5 · NET-DESIGN §1 · errata E-11 |
| CC-1 | ADR-0011, ADR-0013, ADR-0020 · DATA §2/§5 |
| CC-2 | ADR-0011, ADR-0020 · DATA §2.3/§5 · API · SEC T5/T7 |
| CC-3 | ADR-0007, ADR-0020 · DATA §5 · API |
| CC-4 | ADR-0005, ADR-0020 · SEC T13 |
| CC-5 | ADR-0012 §5, ADR-0013 · NET-DESIGN §5 |
| REC-1 | ADR-0011, ADR-0020, ADR-0021 · DATA §2/§3 |
| REC-2 | ADR-0011, ADR-0019, ADR-0021 · DATA §7 |
| REC-3 | ADR-0014 · INTEG §4/§5.2 · DATA §5.2 |
| REC-4 | ADR-0014 · DATA §4 · INTEG §3/§4 |
| REC-5 | ADR-0016 · DATA §10 |
| REC-6 | ADR-0015 · DATA §11 · INTEG §8 |
| REC-7 | ADR-0020 · INTEG §6 · API |
| INT-1 | ADR-0014 · INTEG §4 · DATA §4 · SEC T6b |
| INT-2 | ADR-0020 · INTEG §6 |
| INT-3 | ADR-0011 · PRD §7 FR-36 |
| INT-4 | PRD §7 FR-24 · NET-7 · SEC T20 |
| INT-5 | SEC T6b · INTEG §7 |
| DEP-1 | ADR-0012, ADR-0013, ADR-0014 · NET-DESIGN §6 |
| DEP-2 | ADR-0005, ADR-0009, ADR-0019, ADR-0021 · NET-DESIGN §3 · SEC T2/T14 |
| DEP-3 | ADR-0016 |
| DEP-4 | ADR-0020 · SEC T13 |
| DEP-5 | INTEG §7 · SEC T12 |
| QA-1 | REC-1, CC-2/3/4 · TEST (unit) |
| QA-2 | INT-1..5, NET-6..8 · TEST (integration) |
| QA-3 | API · TEST (contract) |
| QA-4 | ADR-0018 · TEST (perf) |
| QA-5 | SEC §2 (T1–T21) · TEST (security) |
| QA-6 | ADR-0018 · DATA §5.2 |
| QA-7 | PRD §3 (P1–P4) · DSRM Phase 4/5 |
| QA-8 | PRD §3.1 |

**ADR coverage check — all 21 ADRs accounted for:**

- **Superseded, no build item by construction of the successor** (their successor's items cover the
  ground instead): ADR-0001→ADR-0011 (REC-1/CC-1/CC-2); ADR-0003→ADR-0012 (NET-1/2/5/6); ADR-0004→ADR-0013
  (NET-3/7); ADR-0006→ADR-0014 (REC-3/4, INT-1); ADR-0009→ADR-0019 (NET-2/DEP-2); ADR-0010→ADR-0015 (REC-6).
- **Accepted, unaffected, informing but not owning a distinct item:** ADR-0002 (audit-overlay non-goal —
  informs INT-1/DATA §6, no dedicated item); ADR-0005 → NET-2/CC-4/DEP-2; ADR-0007 → CC-1/CC-3/NET-3/QA-4
  (`PT-3` validates it).
- **Accepted (defer), no build item by design:** ADR-0008 (ZKP/Idemix deferred to an evaluation-phase
  exploration — nothing to build unless gap G-07 closes with a real story).
- **Accepted (working recommendation, sponsor-pending S-2), no dedicated build item:** ADR-0017 — "keep
  the existing relational tier, no migration" is a **non-change**; it informs `INT-1`/`DATA` §6 as a
  constraint, not a thing to build.
- **New/amended, each with a distinct item:** ADR-0011 → REC-1/CC-1/CC-2; ADR-0012 → NET-1/2/5/6/8,
  CC-5, DEP-1/2; ADR-0013 → NET-1/3/7, CC-5, DEP-1; ADR-0014 → REC-3/4, INT-1, DEP-1; ADR-0015 → REC-6;
  ADR-0016 → REC-5, DEP-3; ADR-0018 → QA-4/6/8; ADR-0019 → REC-2, DEP-2, QA-5 (`ST-10`); ADR-0020 →
  CC-1..5, REC-1/7, INT-2; ADR-0021 → REC-1/2, QA-1 (`U-2/U-3`), QA-5 (`ST-12`).

**All 21 ADRs accounted for.** Two (ADR-0008, ADR-0017) deliberately have no dedicated build item — one
by an explicit defer decision, one because the decision *is* "change nothing."

---

## 4. DSRM phase mapping (roadmap / G9) & critical path

**Phase → DSRM mapping.** DSRM **Phase 4 (Demonstration) is post-G9** (`[prd: §11.1]` voids the prior
sign-off; re-approval is **S-5**). This backlog executes only after the sponsor lifts **S-1** and
subsequently re-approves **G9**:

| Build phase | DSRM phase | Gate relationship |
|---|---|---|
| §1 governance + pre-build gate (S-1..S-5, PB-1..5) | closes design-phase `[ASSUMPTION]`s and the gate deadlock itself | **S-1 gates everything**; PB-1/PB-3 gate specific items; PB-4/PB-5 already closed |
| 1 Network bring-up · 2 Chaincode · 3 In-band recording · 4 Integration · 5 Deploy | **Phase 4 — Demonstration** | post-**G9** (re-approval, S-5) |
| 6 Test/Eval (`QA-1..3` build-verification; `QA-4..8` evaluation) | **Phase 4 → Phase 5 (Evaluation)** | `QA-7`/`QA-8` are the DSRM Phase-5 evaluation against **P1–P4**, not the retired single G-01 metric |

**Critical path (finish-to-start):**
`S-1 → NET-1 → NET-2 → NET-3 → NET-5 → NET-6 → NET-7 → CC-1 → CC-2 → CC-5 → REC-1 (with PB-3) → REC-3 →
REC-4 → INT-1 → QA-2 → QA-5 → QA-7 → QA-8 → S-5`.

Parallelizable off the critical path: `NET-4`, `NET-8` (once `NET-7`/`CC-5` land), `CC-3/CC-4`, `REC-2`,
`REC-5/REC-6/REC-7`, `INT-2/INT-3/INT-4/INT-5`, `DEP-1..5`, `QA-1/QA-3/QA-4/QA-6`. **`NET-7` is a hard
blocker for `INT-4`, `QA-2` `IT-7/IT-8`, and `QA-5` `ST-14`** — none of those can be satisfied by a
single-tenant network, per errata E-11. **`DEP-2`** (identity/TLS/HSM) must precede **`QA-5`**.

---

## 5. Feeding Jira / the TM board when access returns

Unaffected by this reconciliation wave. This local backlog maps 1:1 onto the `jira-ticketing` / TM-board
(Heimdall) import:

- Each **phase** → an Epic; each **work item** → a Story/Task under it.
- The **`Depends-on`** column → Jira "blocks/is-blocked-by" links; the **`Est`** column → Story Points.
- **`Role`** → component/assignee-group label (`fabric-architect`/`fabric-engineer`/`backend-engineer`/
  `sre`/`qa`/`security-architect`/`architect`); phase → a `phase:<n>-<name>` label; add a
  `feature:hris-fabric-anchor` label per the `pm` spec's `feature:<slug>` convention.
- **§1 (S-1..S-5, PB-1..3)** → a blocking "Governance + pre-build gate" epic whose issues must be **Done**
  before any Phase 1–6 Story moves to *In Progress*.
- The `qa` per-layer items (`QA-1..8`) pair with the `qa-test-plan` skill once tickets exist.

Until Atlassian is reachable this file **is** the backlog of record.

---

## 6. Appendix — retired items (superseded, removed from the live table; kept for audit trail)

Per this rewrite's mandate to remove, not re-mark, dead work (§ "What changed" above). None of these
carry forward story points or open status; they are historical record only.

| Old ID | Old purpose | Disposition |
|---|---|---|
| `NET-3` (pre-2026-08-06) | PDC `collections-config.json` + AND-endorsement for a narrow HR↔Audit field exchange | **No successor.** No PDC anywhere in the ratified design (ADR-0020 §9); endorsement policy now lives in `NET-5`/`CC-5` without a collection. |
| `CC-5` (pre-2026-08-06) | PDC `transient`-data path + `PurgePrivateData`/`blockToLive` erasure | **No successor.** Erasure is off-chain crypto-shred only (`REC-6`, ADR-0015); no on-chain erasure operation exists in this design. |
| `CC-6` (pre-2026-08-06) | Chaincode lifecycle | **Renumbered → `CC-5` (this document).** Same substance, new slot, folded together with endorsement-policy commit. |
| `AS-1` | Scaffold a thin Go anchor-service | **No successor as a service.** ADR-0014 removes the standalone deployable; its layering concerns are absorbed into `REC-3`/`REC-4`, hosted wherever G-10 is eventually answered. |
| `AS-2` | Fabric Gateway client (in the anchor-service) | **Superseded → `REC-3`** (host-agnostic Gateway wrapper). |
| `AS-3` | Commitment builder — flat `pseudonymKey` HMAC, per-changed-event | **Superseded → `REC-1`** (per-section digest, `employeeKey_i`-only per ADR-0011/ADR-0021 — the flat `pseudonymKey` scheme this item built no longer exists). |
| `AS-4` | `anchor_map` off-chain store + crash-safe state machine | **No successor.** No companion store/state-machine is needed; `CC-2`'s own no-op-on-identical-`dataHash` rule is the only replay-safety mechanism this design uses (`INTEG` §5.1). |
| `AS-5` | anchor-service side of the app-layer read-back client | **No successor.** No read-back client exists; **PB-2/G-23** is recorded as likely-dissolved pending **S-4**, not closed. |
| `AS-6` | Kafka consumer wiring (`employee_info`) | **No successor.** Zero Kafka topics in this design (ADR-0014). |
| `AS-7` | Secondary REST surface (`/v1/anchors`, `:verify`, OpenAPI) | **No successor.** No standalone service exists to host such a surface; `API` (`api-contracts.md`) is now a chaincode-function reference, not a REST API (ADR-0020's explicit "what this document deliberately does not contain"). |
| `AS-8` | Erasure hook + drive `PurgePrivateData` | **Superseded → `REC-6`** (crypto-shred only; the `PurgePrivateData` leg has no successor since no PDC exists). |
| `INT-1` (pre-2026-08-06) | Expose/confirm an internal read-back endpoint | **No successor.** No read-back endpoint exists under in-band recording. |
| `INT-2` (pre-2026-08-06) | Confirm the Kafka producer emits `EVENT_UPDATE_PERSONAL` with correlation metadata | **No successor.** No event/Kafka surface exists; the real seam is the five write paths directly (`REC-4`). |
| `INT-3` (pre-2026-08-06) | Wire the e2e anchor write path via Kafka + read-back | **Superseded → `INT-1`** (this document), in-band, no queue. |
| `INT-4` (pre-2026-08-06) | Wire the verify/audit read path | **Superseded → `INT-2`** (this document). |
| `INT-5` (pre-2026-08-06) | Periodic MySQL-change-log ↔ on-chain-anchor reconciliation job | **No successor as a job.** Per `INTEG` §5.1 there is no dual-write gap to reconcile; narrowed to `INT-5` (this document)'s within-request detectability requirement. |

---

*Traceability. Realizes G7 (Tasks + test strategy) alongside the companion
[`./test-strategy.md`](./test-strategy.md). Every build task cites an ADR ([`../05-adr/`](../05-adr/))
and/or a G5 design doc ([`../00-architecture/solution/`](../00-architecture/solution/)); security items
cite G6 ([`../08-security/security-architecture.md`](../08-security/security-architecture.md)). All
phases execute in **DSRM Phase 4 (Demonstration), post-G9-re-approval (S-5)** — no prototype code before
the sponsor lifts **S-1** and the human approval gate is re-granted
([`../README.md`](../README.md), `project-context.md` AUTHORITY NOTICE). Open blockers tracked in
[`../11-execution/grounding-gaps.md`](../11-execution/grounding-gaps.md): G-10 (reopened), G-18, G-19/21/23
(pending S-4), G-24.*
